# What's New in SwiftUI — macOS 26 / iOS 26

> Extracted from Xcode 26.2 SDK · February 2026

**7684 new symbols** across 80 new types and 166 extended types

## New Types

### `AnyCompositorContent`
*struct* · *macOS 26.0, visionOS 26.0*

Type erased compositor content.

```swift
struct AnyCompositorContent
```

### `AssistiveAccess`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

A scene that presents an interface appropriate for Assistive Access on iOS and iPadOS. On other platforms, this scene is unused.

```swift
struct AssistiveAccess<Content> where Content : View
```

### `AttributedTextSelection`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

Represents a selection of attributed text.  A selection is either an insertion point (e.g. a cursor in the text), or spans over a range of characters. While that range is always visually contiguous, it may not be logically contiguous in the text storage. Specifically, a single selection value cannot represent multiple cursors.  This is frequently used to represent selection of text in a `TextEditor`. The following example shows a text editor that leverages text selection to offer live suggestion

```swift
struct AttributedTextSelection
```

### `AttributedTextSelection.Attributes`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

A sequence of all attribute values a selection has in a certain text.  The values of a selection are the attribute values of each run that is fully or partially selected, or the typing attributes in the case the selection is an insertion point.  By default, the sequence contains the attribute container for every run or the typing attributes. Use the ``Attributes``' subscript to obtain only the values for a single attribute:      selection.attributes(in: text)[\.foregroundColor].contains(.red)

```swift
struct Attributes<Text>
```

### `AttributedTextSelection.Indices`
*enum* · *macOS 26.0, iOS 26.0, visionOS 26.0*

The indices of the current selection.

```swift
@frozen enum Indices
```

### `AutomaticImmersionStyle`
*struct* · *macOS 26.0, visionOS 1.0*

The default style of immersive spaces.  You don't typically use this style explicitly, but if you need to, use ``ImmersionStyle/automatic`` with the ``Scene/immersionStyle(selection:in:)``modifier to specify this style.

```swift
struct AutomaticImmersionStyle
```

### `CompositorContent`
*protocol* · *macOS 26.0, visionOS 26.0*

```swift
@MainActor protocol CompositorContent
```

### `CompositorContentBuilder`
*struct* · *macOS 26.0, visionOS 26.0*

A result builder for composing a collection of ``CompositorContent`` elements.

```swift
@resultBuilder struct CompositorContentBuilder
```

### `CompositorContentBuilder.Content`
*struct* · *macOS 26.0, visionOS 26.0*

A representation of the content of a compositor content builder.

```swift
struct Content<C> where C : CompositorContent
```

### `ControlWidget`
*protocol* · *macOS 26.0, iOS 18.0*

The configuration and content of a control widget to display in system spaces  such as Control Center, the Lock Screen, and the Action Button.  Controls allow users to quickly read the state of your app or its accessories, and take quick actions, without having to open your app. Users can add, configure, and arrange controls to suit their individual needs. You can provide multiple types of controls, each representing a specific kind of action.  There are three key components to a control:  * A c

```swift
@MainActor @preconcurrency protocol ControlWidget
```

### `ControlWidgetConfiguration`
*protocol* · *macOS 26.0, iOS 18.0*

A type that describes a control widget's content.  A type conforming to this protocol inherits `@preconcurrency @MainActor` isolation from the protocol if the conformance is included in the type's base declaration:      struct MyCustomType: Transition {         // `@preconcurrency @MainActor` isolation by default     }  Isolation to the main actor is the default, but it's not required. Declare the conformance in an extension to opt out of main actor isolation:      extension MyCustomType: Transi

```swift
@MainActor @preconcurrency protocol ControlWidgetConfiguration
```

### `ControlWidgetConfigurationBuilder`
*struct* · *macOS 26.0, iOS 18.0*

A custom attribute that constructs a control widget's body.  The `@ControlWidgetConfigurationBuilder` attribute allows your control widget's body closure to produce a control widget configuration after zero or more other statements:      struct GarageDoorOpener: ControlWidget {         var body: some ControlWidgetConfiguration {             let kind = "com.yourcompany.GarageDoorOpener"              StaticControlConfiguration(                 kind: kind             ) {                 ...        

```swift
@resultBuilder struct ControlWidgetConfigurationBuilder
```

### `ControlWidgetTemplate`
*protocol* · *macOS 26.0, iOS 18.0*

A type that describes a control widget's content.  Controls are defined using templates in order to ensure that they control will work at all sizes and in all system spaces in which they might be displayed. These templates define images (specifically, symbol images) and text using simple SwiftUI views like ``Label``, ``Text``, and ``Image``; and tint colors using the ``ControlWidgetTemplate/tint(_:)`` modifier.  A type conforming to this protocol inherits `@preconcurrency @MainActor` isolation f

```swift
@MainActor @preconcurrency protocol ControlWidgetTemplate
```

### `ControlWidgetTemplateBuilder`
*struct* · *macOS 26.0, iOS 18.0*

A custom attribute that constructs a control widget template's body.  The `@ControlWidgetTemplateBuilder` attribute allows your control template's body closure to produce a control template after zero or more other statements:      struct GarageDoorOpener: ControlWidget {         var body: some ControlWidgetConfiguration {             let kind = "com.yourcompany.GarageDoorOpener"              StaticControlConfiguration(                 kind: kind             ) {                 let isOpen = ... 

```swift
@resultBuilder struct ControlWidgetTemplateBuilder
```

### `DefaultButtonLabel`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

The default label to use for a button.  You don't use this type directly. Instead, the system creates it automatically when you construct certain types of `Button`.

```swift
struct DefaultButtonLabel
```

### `DefaultToolbarItem`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

A toolbar item that represents a system component.  Place this item in your toolbar to control where the system-provided item, like search, will be positioned.

```swift
struct DefaultToolbarItem
```

### `DismissImmersiveSpaceAction`
*struct* · *macOS 26.0, visionOS 1.0*

An action that dismisses an immersive space.  Use the ``EnvironmentValues/dismissImmersiveSpace`` environment value to get an instance of this type for a given ``Environment``. Then call the instance to dismiss a space. You call the instance directly because it defines a ``DismissImmersiveSpaceAction/callAsFunction()`` method that Swift calls when you call the instance.  On macOS, this may be used to dismiss a remote immersive space declared with ``RemoteImmersiveSpace``.  For example, you can d

```swift
@MainActor struct DismissImmersiveSpaceAction
```

### `DragConfiguration`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

The behavior of the drag, proposed by the dragging source.

```swift
struct DragConfiguration
```

### `DragConfiguration.OperationsOutsideApp`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

Describes the suggested drag operations to other applications.  To create a default configuration, initialize it without parameters.  On iOS, the default behavior is to disallow drag outside the application. On macOS—support drag-to-copy to destinations both within the application and to other apps.  In addition to `copy`, add `move` operation support by specifying that in the initializer:         struct DraggableBookView: View {            var id: UUID             var body: some View {         

```swift
struct OperationsOutsideApp
```

### `DragConfiguration.OperationsWithinApp`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

Describes the drag operations suggested to destinations within the app.  To create a default configuration, initialize it without parameters.  On iOS, the default behavior is to allow drag-to-copy within the application. On macOS, the default configuration is to support drag-to-copy to destinations both within the application and to other apps.  In addition to `copy`, add `move` operation support by specifying that in the initializer:         struct DraggableBookView: View {            var id: U

```swift
struct OperationsWithinApp
```

### `DragDropPreviewsFormation`
*struct* · *macOS 26.0*

On macOS, describes the way the dragged previews are visually composed. Both drag sources and drop destination can specify their desired preview formation.

```swift
struct DragDropPreviewsFormation
```

### `DragSession`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

Describes the ongoing dragging session.

```swift
struct DragSession
```

### `DragSession.ID`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

The identifier of a drag session.

```swift
struct ID
```

### `DragSession.Phase`
*enum* · *macOS 26.0, iOS 26.0, visionOS 26.0*

The phase of the current drag session

```swift
enum Phase
```

### `DropConfiguration`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

Describes the behavior of the drop.

```swift
struct DropConfiguration
```

### `DropOperation.Set`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

A set of drop operations, corresponding to matching cases in `DropOperation`.

```swift
struct Set
```

### `DropSession`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

```swift
struct DropSession
```

### `DropSession.ID`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

The identifier of a drag session.

```swift
struct ID
```

### `DropSession.LocalSession`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

Describes the session originated within the app.

```swift
struct LocalSession
```

### `DropSession.Phase`
*enum* · *macOS 26.0, iOS 26.0, visionOS 26.0*

The phase of the current drop session.

```swift
enum Phase
```

### `EmptyControlWidgetConfiguration`
*struct* · *macOS 26.0, iOS 18.0*

An empty control widget configuration.

```swift
@MainActor @frozen @preconcurrency struct EmptyControlWidgetConfiguration
```

### `EmptyControlWidgetTemplate`
*struct* · *macOS 26.0, iOS 18.0*

An empty control widget template.

```swift
@MainActor @frozen @preconcurrency struct EmptyControlWidgetTemplate
```

### `FindContext`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

The status of the find navigator for views which support text editing.  Views which support text editing can use this information to implement a a find navigator that is controlled using the modifiers used for controlling the find navigator throughout the rest of SwiftUI.  For example, the following shows a minimal find navigator implementation driven by the find context which falls back to local state if no `isPresented` binding is provided:      struct FindNavigatorDrivenTextInput: View {     

```swift
struct FindContext
```

### `FullImmersionStyle`
*struct* · *macOS 26.0, visionOS 1.0*

An immersion style that displays unbounded content that completely replaces passthrough video.  When this immersion style is selected, the immersion amount reported by the closure of ``View/onImmersionChange(initial:_:)`` is `1.0`.  Use ``ImmersionStyle/full`` with the ``Scene/immersionStyle(selection:in:)``modifier to specify this style.

```swift
struct FullImmersionStyle
```

### `GlassButtonStyle`
*struct* · *macOS 26.0, iOS 26.0*

A button style that applies glass border artwork based on the button's context.  You can also use ``PrimitiveButtonStyle/glass`` to construct this style.

```swift
struct GlassButtonStyle
```

### `GlassProminentButtonStyle`
*struct* · *macOS 26.0, iOS 26.0*

A button style that applies prominent glass border artwork based on the button's context.  You can also use ``PrimitiveButtonStyle/glassProminent`` to construct this style.

```swift
struct GlassProminentButtonStyle
```

### `ImmersionChangeContext`
*struct* · *macOS 26.0, visionOS 2.0*

A structure that represents a state of immersion of your app.  You don't use this structure directly. Instead, SwiftUI provides instances of this structure via the `onImmersionChange` modifier's closure.

```swift
struct ImmersionChangeContext
```

### `ImmersionStyle`
*protocol* · *macOS 26.0, visionOS 1.0*

The styles that an immersive space can have.  Configure the appearance and behavior of an ``ImmersiveSpace`` by adding the ``Scene/immersionStyle(selection:in:)`` scene modifier to the space and specifying a style that conforms to this protocol, like ``ImmersionStyle/mixed`` or ``ImmersionStyle/full``. For example, the following app defines a solar system scene that uses full immersion:      @main     struct SolarSystemApp: App {         @State private var style: ImmersionStyle = .full          

```swift
protocol ImmersionStyle
```

### `ImmersiveSpaceContent`
*protocol* · *macOS 26.0, visionOS 1.0*

A type that you can use as the content of an immersive space.  A type conforming to this protocol inherits `@preconcurrency @MainActor` isolation from the protocol if the conformance is included in the type's base declaration:      struct MyCustomType: Transition {         // `@preconcurrency @MainActor` isolation by default     }  Isolation to the main actor is the default, but it's not required. Declare the conformance in an extension to opt out of main actor isolation:      extension MyCustom

```swift
@MainActor @preconcurrency protocol ImmersiveSpaceContent
```

### `ImmersiveSpaceContentBuilder`
*struct* · *macOS 26.0, visionOS 1.0*

A result builder for composing a collection of immersive space elements.

```swift
@resultBuilder struct ImmersiveSpaceContentBuilder
```

### `NSGestureRecognizerRepresentable`
*protocol* · *macOS 26.0*

A wrapper for an `NSGestureRecognizer` that you use to integrate that gesture recognizer into your SwiftUI hierarchy.  Use an ``NSGestureRecognizerRepresentable`` instance to create and manage an <doc://com.apple.documentation/documentation/AppKit/NSGestureRecognizer> object in your SwiftUI interface.  To add your gesture recognizer to a SwiftUI view, create an instance of ``NSGestureRecognizerRepresentable`` and use the ``View/gesture(_:)`` modifier to attach it. The system calls the methods of

```swift
@MainActor @preconcurrency protocol NSGestureRecognizerRepresentable
```

### `NSGestureRecognizerRepresentableContext`
*struct* · *macOS 26.0*

Contextual information about the state of the system that you use to create and update a represented gesture recognizer.

```swift
struct NSGestureRecognizerRepresentableContext<Representable> where Representable : NSGestureRecognizerRepresentable
```

### `NSGestureRecognizerRepresentableCoordinateSpaceConverter`
*struct* · *macOS 26.0*

A structure used to convert locations to and from coordinate spaces in the hierarchy of the SwiftUI view associated with an ``NSGestureRecognizerRepresentable``.

```swift
struct NSGestureRecognizerRepresentableCoordinateSpaceConverter
```

### `NSHostingSceneRepresentation`
*class* · *macOS 26.0*

An AppKit type that hosts and can present SwiftUI scenes  Use instances of this type with ``NSApplication.addSceneRepresentation(_:)`` to include SwiftUI scene functionality in an app which uses the AppKit app lifecycle.  For example, you can add a `Settings` scene to your app and present it when the corresponding menu item is selected:      import AppKit     import SwiftUI      @main     class ApplicationDelegate: NSApplicationDelegate {         let settingsScene = NSHostingSceneRepresentation 

```swift
@MainActor class NSHostingSceneRepresentation<Content> where Content : Scene
```

### `OpenImmersiveSpaceAction`
*struct* · *macOS 26.0, visionOS 1.0*

An action that presents an immersive space.  Use the ``EnvironmentValues/openImmersiveSpace`` environment value to get the instance of this structure for a given ``Environment``. Then call the instance to present a space. You call the instance directly because it defines `callAsFunction()` methods that Swift calls when you call the instance.  On macOS, this may be used to open a remote immersive space declared with ``RemoteImmersiveSpace``. When your app opens a remote immersive space, the syste

```swift
@MainActor struct OpenImmersiveSpaceAction
```

### `OpenImmersiveSpaceAction.Result`
*enum* · *macOS 26.0, visionOS 1.0*

The outcome of an attempt to open an immersive space.

```swift
enum Result
```

### `PencilDoubleTapGestureValue`
*struct* · *macOS 14.5, iOS 17.5, visionOS 26.2*

Describes the value of an Apple Pencil double-tap gesture.

```swift
struct PencilDoubleTapGestureValue
```

### `PencilHoverPose`
*struct* · *macOS 14.5, iOS 17.5, visionOS 26.2*

A value describing the location and distance of an Apple Pencil hovering in the area above a view’s bounds.

```swift
struct PencilHoverPose
```

### `PencilPreferredAction`
*struct* · *macOS 14.5, iOS 17.5, visionOS 26.2*

An action that the user prefers to perform after double-tapping their Apple Pencil.

```swift
struct PencilPreferredAction
```

### `PencilSqueezeGesturePhase`
*enum* · *macOS 14.5, iOS 17.5, visionOS 26.2*

Describes the phase and value of an Apple Pencil squeeze gesture.  When you use the ``View/onPencilSqueeze(perform:)`` view modifier, you can handle the Apple Pencil squeeze gesture’s phase in the `action` closure.

```swift
@frozen enum PencilSqueezeGesturePhase
```

### `PencilSqueezeGestureValue`
*struct* · *macOS 14.5, iOS 17.5, visionOS 26.2*

Describes the value of an Apple Pencil squeeze gesture.

```swift
struct PencilSqueezeGestureValue
```

### `ProgressiveImmersionAspectRatio`
*struct* · *macOS 26.0, visionOS 26.0*

```swift
struct ProgressiveImmersionAspectRatio
```

### `ProgressiveImmersionStyle`
*struct* · *macOS 26.0, visionOS 1.0*

An immersion style that displays unbounded content that partially replaces passthrough video.  Use ``ImmersionStyle/progressive`` with the ``Scene/immersionStyle(selection:in:)``modifier to specify this style.

```swift
struct ProgressiveImmersionStyle
```

### `RemoteDeviceIdentifier`
*struct* · *macOS 26.0, visionOS 26.0*

An opaque type that identifies a remote device displaying scene content in a ``RemoteImmersiveSpace``.  Access this from the ``EnvironmentValues/remoteDeviceIdentifier`` environment property in a remote scene to get the identifier for that scene's device.  When accessed in a context that is being presented on the local device, this value will be `nil`.  This identifier can also be used to initialize an ``ARKitSession`` associated with the remote device.      struct SolarSystem: CompositorContent

```swift
struct RemoteDeviceIdentifier
```

### `RemoteImmersiveSpace`
*struct* · *macOS 26.0*

A scene that presents its content in an unbounded space on a remote device.  Use a remote immersive space as a container for compositor content that your macOS app presents on a user's chosen visionOS device. The compositor content that you declare as the remote immersive space's content serves as a template for it:      @main     struct SolarSystemApp: App {         var body: some Scene {             RemoteImmersiveSpace {                 CompositorLayer { layerRenderer in                     /

```swift
struct RemoteImmersiveSpace<Content, Data> where Content : ImmersiveSpaceContent, Data : Decodable, Data : Encodable, Data : Hashable
```

### `SceneLaunchBehavior`
*struct* · *macOS 15.0, visionOS 26.0*

The launch behavior for a scene.  Use the ``Scene/defaultLaunchBehavior(_:)`` modifier to apply a value of this type to a ``Scene`` you specify in your ``App``. The value you specify determines how the system will present the scene in the absense of any previously restored scenes on launch of your application.  For example, you may wish to present a welcome window on launch of your app when there are no previous document windows being restored:      @main     struct MyApp: App {         var body

```swift
struct SceneLaunchBehavior
```

### `ScrollEdgeEffectStyle`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

A structure that defines the style of pocket a scroll view will have.

```swift
struct ScrollEdgeEffectStyle
```

### `SearchToolbarBehavior`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

The behavior of a search field in a toolbar.  Use this type in combination with the ``View/searchToolbarBehavior(_:)`` modifier.

```swift
struct SearchToolbarBehavior
```

### `SensoryFeedback`
*struct* · *macOS 14.0, iOS 17.0, visionOS 26.0*

Represents a type of haptic and/or audio feedback that can be played.  This feedback can be passed to `View.sensoryFeedback` to play it.

```swift
struct SensoryFeedback
```

### `SensoryFeedback.Flexibility`
*struct* · *macOS 14.0, iOS 17.0, visionOS 26.0*

The flexibility to be represented by a type of feedback.  `Flexibility` values can be passed to `SensoryFeedback.impact(flexibility:intensity:)`.

```swift
struct Flexibility
```

### `SensoryFeedback.PressFeedback`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

Feedback that can be played in response to a press (touch down) on a control.

```swift
struct PressFeedback
```

### `SensoryFeedback.ReleaseFeedback`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

Feedback that can be played in response to a release (touch up) of a control.

```swift
struct ReleaseFeedback
```

### `SensoryFeedback.SelectionFeedback`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

Feedback that can be played in response to a specific UI element's values changing.

```swift
struct SelectionFeedback
```

### `SensoryFeedback.Weight`
*struct* · *macOS 14.0, iOS 17.0, visionOS 26.0*

The weight to be represented by a type of feedback.  `Weight` values can be passed to `SensoryFeedback.impact(weight:intensity:)`.

```swift
struct Weight
```

### `SliderTick`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

A representation of a tick in a slider, with associated value and optional label.  The following example shows a slider bound to the value `percentage`. As the slider updates the `currentValueLabel`. The slider also renders marks at a `0.25` step interval.      @State private var percentage = 0.5      Slider(value: $percentage) {         Text("Percentage")     } currentValueLabel: {         Text("\(percentage)%")     } ticks: {         SliderTickContentForEach(             stride(from: 0.0, thro

```swift
struct SliderTick<V> where V : BinaryFloatingPoint
```

### `SliderTick.ID`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

The identity of a tick.

```swift
struct ID
```

### `SliderTickBuilder`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

A result builder that constructs `SliderTick`s for use when creating a `Slider`.

```swift
@resultBuilder struct SliderTickBuilder<V> where V : BinaryFloatingPoint
```

### `SliderTickContent`
*protocol* · *macOS 26.0, iOS 26.0, visionOS 26.0*

A type that provides content for a `SliderTickBuilder`.

```swift
protocol SliderTickContent<Value>
```

### `SliderTickContentForEach`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

A type of slider content that creates content by iterating over a collection.

```swift
struct SliderTickContentForEach<Data, ID, Content> where Data : RandomAccessCollection, ID : Hashable, Content : SliderTickContent
```

### `SpacerSizing`
*struct* · *macOS 26.0, iOS 26.0*

A type which defines how spacers should size themselves.  Use this type in coordination with the ``ToolbarSpacer`` type to define if the spacer should be a flexible size, or a fixed size using system-defined sizing rules.  For example, the following adds a fixed-size toolbar spacer between the share and more buttons in the toolbar:      ContentView()         .toolbar(id: "main-toolbar") {             ToolbarItem(id: "tag") {                TagButton()             }             ToolbarItem(id: "s

```swift
struct SpacerSizing
```

### `SurroundingsEffect`
*struct* · *macOS 26.0, visionOS 1.0*

Effects that the system can apply to passthrough video.  Use one of these values with the ``View/preferredSurroundingsEffect(_:)`` view modifier to indicate what effect to apply to passthrough video when the modified view is displayed.

```swift
struct SurroundingsEffect
```

### `TabBarMinimizeBehavior`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

```swift
struct TabBarMinimizeBehavior
```

### `TabSearchActivation`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

Configures the activation behavior of search in the search tab.  You can configure this for a particular ``TabView`` by using the ``View/tabViewSearchActivation(_:)`` modifier.

```swift
struct TabSearchActivation
```

### `TabViewBottomAccessoryPlacement`
*enum* · *macOS 26.0, iOS 26.0, visionOS 26.0*

A placement of the bottom accessory in a tab view. You can use this to adjust the content of the accessory view based on the placement.  The following example shows playback controls when the view is inline, and an expanded slider player view when the view is expanded.      struct MusicPlaybackView: View {         @Environment(\.tabViewBottomAccessoryPlacement) var placement          var body: some View {             switch placement {             case .inline:                 ControlsPlaybackVi

```swift
enum TabViewBottomAccessoryPlacement
```

### `TextInputFormattingControlPlacement`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

A structure defining the system text formatting controls available on each platform.

```swift
struct TextInputFormattingControlPlacement
```

### `TextInputFormattingControlPlacement.Set`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

A set of system text formatting controls.

```swift
struct Set
```

### `ToolbarSpacer`
*struct* · *macOS 26.0, iOS 26.0*

A standard space item in toolbars.  A space item creates visual breaks in the toolbar between items. Spacers can have a standard fixed size or be flexible and push items apart.  Spacers can also be used in customizable toolbars:      ContentView()         .toolbar(id: "main-toolbar") {             ToolbarItem(id: "tag") {                TagButton()             }             ToolbarItem(id: "share") {                ShareButton()             }             ToolbarSpacer(.fixed)             Toolbar

```swift
struct ToolbarSpacer
```

### `TupleSliderTickContent`
*struct* · *macOS 26.0, iOS 26.0, visionOS 26.0*

Slider content created from a Swift tuple of slider content.

```swift
@frozen struct TupleSliderTickContent<V, T> where V : BinaryFloatingPoint
```

### `Window`
*struct* · *macOS 13.0, visionOS 26.0*

A scene that presents its content in a single, unique window.  Use a `Window` scene to augment the main interface of your app with a window that gives people access to supplemental functionality. For example, you can create a secondary window in a mail reader app that enables people to view the status of their account connections:       @main      struct Mail: App {          var body: some Scene {              WindowGroup {                  MailViewer()              }              Window("Connec

```swift
struct Window<Content> where Content : View
```

### `WorldTrackingLimitation`
*struct* · *macOS 26.0, visionOS 2.0*

A structure to represent limitations of tracking the user's surroundings.  You receive a set of world tracking limitations when you read the ``EnvironmentValues/worldTrackingLimitations`` environment value. The value tells you which limitations the device currently is facing. If any of the limitations occur due to changing circumstances, e.g., the lighting, the set is updated accordingly. For example, the following ``Text`` view automatically updates when the world tracking limitations change:  

```swift
struct WorldTrackingLimitation
```

---

## New Members on Existing Types

### `AlertScene` — 2 new members

**`defaultLaunchBehavior(_:)`** — Sets the default launch behavior for this scene.  This behavior can be used to define if a scene is shown on application
```swift
nonisolated func defaultLaunchBehavior(_ behavior: SceneLaunchBehavior) -> some Scene
```
**`immersionStyle(selection:in:)`** — Sets the style for an immersive space.  Use this modifier to configure the appearance and behavior of an ``ImmersiveSpac
```swift
nonisolated func immersionStyle(selection: Binding<any ImmersionStyle>, in styles: any ImmersionStyle...) -> some Scene
```

### `AnyCompositorContent` — 14 new members

**`AnyCompositorContent.Body`**
```swift
typealias Body = Never
```
**`contentCaptureProtected(_:)`** — Marks the view as a view that activates content protection during scene capture events, such as screenshots, screen reco
```swift
nonisolated func contentCaptureProtected(_ isActive: Bool = true) -> some CompositorContent
```
**`init(_:)`** — Create an instance that type-erases `CompositorContent`.
```swift
init<T>(_ content: T) where T : CompositorContent
```
**`init(erasing:)`**
```swift
init<T>(erasing: T) where T : CompositorContent
```
**`onAppear(perform:)`** — Adds an action to perform before this content appears.  The exact moment that SwiftUI calls this method depends on the s
```swift
nonisolated func onAppear(perform action: (() -> Void)? = nil) -> some CompositorContent
```
**`onChange(of:initial:_:)`**
```swift
nonisolated func onChange<V>(of value: V, initial: Bool = false, _ action: @escaping (V, V) -> Void) -> some CompositorContent where V : Equatable
```
**`onChange(of:initial:_:)`**
```swift
nonisolated func onChange<V>(of value: V, initial: Bool = false, _ action: @escaping () -> Void) -> some CompositorContent where V : Equatable
```
**`onDisappear(perform:)`** — Adds an action to perform after this content disappears.  The exact moment that SwiftUI calls this method depends on the
```swift
nonisolated func onDisappear(perform action: (() -> Void)? = nil) -> some CompositorContent
```
**`onImmersionChange(initial:_:)`** — Performs an action when the immersion state of your app changes.  Depending on the immersion style used for the Immersiv
```swift
nonisolated func onImmersionChange(initial: Bool = true, _ action: @escaping (ImmersionChangeContext, ImmersionChangeContext) -> Void) -> some CompositorContent
```
**`onWorldRecenter(action:)`** — Adds an action to perform when recentering the content with the digital crown.  When the user recenters their content, f
```swift
nonisolated func onWorldRecenter(action: @escaping @MainActor (WorldRecenterPhase) -> Void) -> some CompositorContent
```
**`onWorldRecenter(action:)`** — Adds an action to perform when recentering the view with the digital crown.  When the user recenters their content, the 
```swift
nonisolated func onWorldRecenter(action: @escaping @MainActor () -> Void) -> some CompositorContent
```
**`persistentSystemOverlays(_:)`** — Sets the preferred visibility of the non-transient system views overlaying the app.  Use this modifier to influence the 
```swift
nonisolated func persistentSystemOverlays(_ visibility: Visibility) -> some CompositorContent
```
**`preferredSurroundingsEffect(_:)`** — Applies an effect to passthrough video.  Use this modifier to indicate a preference for the appearance of passthrough vi
```swift
nonisolated func preferredSurroundingsEffect(_ effect: SurroundingsEffect?) -> some CompositorContent
```
**`upperLimbVisibility(_:)`** — Sets the preferred visibility of the user's upper limbs, while an ``ImmersiveSpace`` scene is presented.  The system can
```swift
nonisolated func upperLimbVisibility(_ preferredVisibility: Visibility) -> some CompositorContent
```

### `AssistiveAccess` — 5 new members

**`AssistiveAccess.Body`** — The type of scene that represents the body of this scene.  When you create a custom scene, Swift infers this type from y
```swift
typealias Body = some Scene
```
**`body`** — The content and behavior of the scene.  For any scene that you create, provide a computed `body` property that defines t
```swift
@MainActor @preconcurrency var body: some Scene { get }
```
**`defaultLaunchBehavior(_:)`** — Sets the default launch behavior for this scene.  This behavior can be used to define if a scene is shown on application
```swift
nonisolated func defaultLaunchBehavior(_ behavior: SceneLaunchBehavior) -> some Scene
```
**`immersionStyle(selection:in:)`** — Sets the style for an immersive space.  Use this modifier to configure the appearance and behavior of an ``ImmersiveSpac
```swift
nonisolated func immersionStyle(selection: Binding<any ImmersionStyle>, in styles: any ImmersionStyle...) -> some Scene
```
**`init(content:)`** — Creates an Assistive Access scene.  When Assistive Access is enabled, the given view is used as the root view of the app
```swift
init(@ViewBuilder content: () -> Content)
```

### `AsyncImage` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `AttributedTextSelection` — 17 new members

**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: AttributedTextSelection.Indices, b: AttributedTextSelection.Indices) -> Bool
```
**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: AttributedTextSelection, b: AttributedTextSelection) -> Bool
```
**`AttributedTextSelection.Attributes.Element`** — A type representing the sequence's elements.
```swift
typealias Element = AttributeContainer
```
**`AttributedTextSelection.Attributes.Iterator`** — A type that provides the sequence's iteration interface and encapsulates its iteration state.
```swift
typealias Iterator = some IteratorProtocol<AttributeContainer>
```
**`AttributedTextSelection.Indices.insertionPoint(_:)`** — The index of a single insertion point.  The an insertion point at the `startIndex` of an attributed string is equivalent
```swift
case insertionPoint(AttributedString.Index)
```
**`AttributedTextSelection.Indices.ranges(_:)`** — The range-set of selected characters.
```swift
case ranges(RangeSet<AttributedString.Index>)
```
**`affinity(in:)`** — Return the selection affinity of the selection.
```swift
func affinity(in text: AttributedString) -> TextSelectionAffinity
```
**`attributes(in:)`** — Obtain a lazy sequence of all attribute values the selection has in a given text.  The attribute values of a selection a
```swift
func attributes(in text: AttributedString) -> AttributedTextSelection.Attributes<AttributedString>
```
**`indices(in:)`** — The current text selection indices.  Always make sure to keep selection and text synchronized. Use ``Foundation/Attribut
```swift
func indices(in text: AttributedString) -> AttributedTextSelection.Indices
```
**`init()`** — Initialize the default selection for a new text editor.  The default selection is an insertion point at the end of the t
```swift
init()
```
**`init(insertionPoint:typingAttributes:)`** — Initialize a selection to a single insertion point.  - Parameter insertionPoint: The index of the string where the chare
```swift
init(insertionPoint: AttributedString.Index, typingAttributes: AttributeContainer? = nil)
```
**`init(range:)`** — Initialize a selection to a character range.
```swift
init(range: Range<AttributedString.Index>)
```
**`init(ranges:)`** — Initialize a selection to character ranges.
```swift
init(ranges: RangeSet<AttributedString.Index>)
```
**`makeIterator()`** — Returns an iterator over the elements of this sequence.
```swift
func makeIterator() -> some IteratorProtocol<AttributeContainer>
```
**`subscript(_:)`** — Returns a sequence which iterates of the values of a single attribute.  In the case of a range selection, the returned s
```swift
subscript<K>(type: KeyPath<AttributeDynamicLookup, K>) -> some Sequence<Optional<K.Value>> where K : AttributedStringKey, K.Value : Sendable { get }
```
**`subscript(_:)`** — Returns a sequence which iterates of the values of a single attribute.  In the case of a range selection, the returned s
```swift
subscript<K>(type: K.Type) -> some Sequence<Optional<K.Value>> where K : AttributedStringKey, K.Value : Sendable { get }
```
**`typingAttributes(in:)`** — Returns the typing attributes for a corresponding text.  The typing attributes are the attributes that will be applied t
```swift
func typingAttributes(in text: AttributedString) -> AttributeContainer
```

### `AutomaticImmersionStyle` — 2 new members

**`automatic`** — The default immersion style.  The system uses this style for an ``ImmersiveSpace`` if you don't provide an ``Scene/immer
```swift
static var automatic: AutomaticImmersionStyle { get }
```
**`init()`**
```swift
init()
```

### `AutomaticTextEditorStyle` — 74 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `Button` — 76 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`init(role:action:)`** — Creates a button that displays a default label.  For example, the following view would display a button with a 'x' symbo
```swift
@preconcurrency init(role: ButtonRole, action: @escaping @MainActor () -> Void)
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `ButtonBorderShape` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `ButtonRole` — 2 new members

**`close`** — A role that indicates a button that closes the current operation.  Unlike a cancel operation, a close operation doesn't 
```swift
static let close: ButtonRole
```
**`confirm`** — A role that indicates a button that confirms an operation.  The following view would display a confirm button in the too
```swift
static let confirm: ButtonRole
```

### `ButtonStyleConfiguration` — 74 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `ColorPicker` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `CompositorContent` — 13 new members

**`Body`**
```swift
associatedtype Body : CompositorContent
```
**`body`**
```swift
@CompositorContentBuilder @MainActor var body: Self.Body { get }
```
**`contentCaptureProtected(_:)`** — Marks the view as a view that activates content protection during scene capture events, such as screenshots, screen reco
```swift
nonisolated func contentCaptureProtected(_ isActive: Bool = true) -> some CompositorContent
```
**`onAppear(perform:)`** — Adds an action to perform before this content appears.  The exact moment that SwiftUI calls this method depends on the s
```swift
nonisolated func onAppear(perform action: (() -> Void)? = nil) -> some CompositorContent
```
**`onChange(of:initial:_:)`**
```swift
nonisolated func onChange<V>(of value: V, initial: Bool = false, _ action: @escaping (V, V) -> Void) -> some CompositorContent where V : Equatable
```
**`onChange(of:initial:_:)`**
```swift
nonisolated func onChange<V>(of value: V, initial: Bool = false, _ action: @escaping () -> Void) -> some CompositorContent where V : Equatable
```
**`onDisappear(perform:)`** — Adds an action to perform after this content disappears.  The exact moment that SwiftUI calls this method depends on the
```swift
nonisolated func onDisappear(perform action: (() -> Void)? = nil) -> some CompositorContent
```
**`onImmersionChange(initial:_:)`** — Performs an action when the immersion state of your app changes.  Depending on the immersion style used for the Immersiv
```swift
nonisolated func onImmersionChange(initial: Bool = true, _ action: @escaping (ImmersionChangeContext, ImmersionChangeContext) -> Void) -> some CompositorContent
```
**`onWorldRecenter(action:)`** — Adds an action to perform when recentering the content with the digital crown.  When the user recenters their content, f
```swift
nonisolated func onWorldRecenter(action: @escaping @MainActor (WorldRecenterPhase) -> Void) -> some CompositorContent
```
**`onWorldRecenter(action:)`** — Adds an action to perform when recentering the view with the digital crown.  When the user recenters their content, the 
```swift
nonisolated func onWorldRecenter(action: @escaping @MainActor () -> Void) -> some CompositorContent
```
**`persistentSystemOverlays(_:)`** — Sets the preferred visibility of the non-transient system views overlaying the app.  Use this modifier to influence the 
```swift
nonisolated func persistentSystemOverlays(_ visibility: Visibility) -> some CompositorContent
```
**`preferredSurroundingsEffect(_:)`** — Applies an effect to passthrough video.  Use this modifier to indicate a preference for the appearance of passthrough vi
```swift
nonisolated func preferredSurroundingsEffect(_ effect: SurroundingsEffect?) -> some CompositorContent
```
**`upperLimbVisibility(_:)`** — Sets the preferred visibility of the user's upper limbs, while an ``ImmersiveSpace`` scene is presented.  The system can
```swift
nonisolated func upperLimbVisibility(_ preferredVisibility: Visibility) -> some CompositorContent
```

### `CompositorContentBuilder` — 19 new members

**`CompositorContentBuilder.Content.Body`**
```swift
typealias Body = Never
```
**`body`**
```swift
var body: Never { get }
```
**`buildBlock(_:)`**
```swift
static func buildBlock<C>(_ content: C) -> C where C : CompositorContent
```
**`buildEither(first:)`** — Produces content for a conditional statement in a multi-statement closure when the condition is true.  Conditional state
```swift
static func buildEither<F>(first: _LimitedAvailabilityCompositorContent) -> _ConditionalContent<_LimitedAvailabilityCompositorContent, F> where F : CompositorContent
```
**`buildEither(first:)`** — Produces content for a conditional statement in a multi-statement closure when the condition is true.  Conditional state
**`buildEither(second:)`** — Produces content for a conditional statement in a multi-statement closure when the condition is false.  Conditional stat
```swift
static func buildEither<F>(second: F) -> _ConditionalContent<_LimitedAvailabilityCompositorContent, F> where F : CompositorContent
```
**`buildExpression(_:)`**
```swift
static func buildExpression<C>(_ content: C) -> C where C : CompositorContent
```
**`buildLimitedAvailability(_:)`** — Processes scene content for a conditional compiler-control statement that performs an availability check.
```swift
static func buildLimitedAvailability(_ content: some CompositorContent) -> _LimitedAvailabilityCompositorContent
```
**`contentCaptureProtected(_:)`** — Marks the view as a view that activates content protection during scene capture events, such as screenshots, screen reco
```swift
nonisolated func contentCaptureProtected(_ isActive: Bool = true) -> some CompositorContent
```
**`onAppear(perform:)`** — Adds an action to perform before this content appears.  The exact moment that SwiftUI calls this method depends on the s
```swift
nonisolated func onAppear(perform action: (() -> Void)? = nil) -> some CompositorContent
```
**`onChange(of:initial:_:)`**
```swift
nonisolated func onChange<V>(of value: V, initial: Bool = false, _ action: @escaping (V, V) -> Void) -> some CompositorContent where V : Equatable
```
**`onChange(of:initial:_:)`**
```swift
nonisolated func onChange<V>(of value: V, initial: Bool = false, _ action: @escaping () -> Void) -> some CompositorContent where V : Equatable
```
**`onDisappear(perform:)`** — Adds an action to perform after this content disappears.  The exact moment that SwiftUI calls this method depends on the
```swift
nonisolated func onDisappear(perform action: (() -> Void)? = nil) -> some CompositorContent
```
**`onImmersionChange(initial:_:)`** — Performs an action when the immersion state of your app changes.  Depending on the immersion style used for the Immersiv
```swift
nonisolated func onImmersionChange(initial: Bool = true, _ action: @escaping (ImmersionChangeContext, ImmersionChangeContext) -> Void) -> some CompositorContent
```
**`onWorldRecenter(action:)`** — Adds an action to perform when recentering the content with the digital crown.  When the user recenters their content, f
```swift
nonisolated func onWorldRecenter(action: @escaping @MainActor (WorldRecenterPhase) -> Void) -> some CompositorContent
```
**`onWorldRecenter(action:)`** — Adds an action to perform when recentering the view with the digital crown.  When the user recenters their content, the 
```swift
nonisolated func onWorldRecenter(action: @escaping @MainActor () -> Void) -> some CompositorContent
```
**`persistentSystemOverlays(_:)`** — Sets the preferred visibility of the non-transient system views overlaying the app.  Use this modifier to influence the 
```swift
nonisolated func persistentSystemOverlays(_ visibility: Visibility) -> some CompositorContent
```
**`preferredSurroundingsEffect(_:)`** — Applies an effect to passthrough video.  Use this modifier to indicate a preference for the appearance of passthrough vi
```swift
nonisolated func preferredSurroundingsEffect(_ effect: SurroundingsEffect?) -> some CompositorContent
```
**`upperLimbVisibility(_:)`** — Sets the preferred visibility of the user's upper limbs, while an ``ImmersiveSpace`` scene is presented.  The system can
```swift
nonisolated func upperLimbVisibility(_ preferredVisibility: Visibility) -> some CompositorContent
```

### `ContentUnavailableView` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `ControlGroup` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `ControlGroupStyleConfiguration` — 148 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `ControlWidget` — 3 new members

**`Body`** — The type of configuration representing the content of this control.  When you create a custom control, Swift infers this
```swift
associatedtype Body : ControlWidgetConfiguration
```
**`body`** — The content and behavior of the control.  For any controls that you create, provide a computed `body` property that defi
```swift
@ControlWidgetConfigurationBuilder @MainActor @preconcurrency var body: Self.Body { get }
```
**`init()`** — Creates a control using `body` as its content.
```swift
@MainActor @preconcurrency init()
```

### `ControlWidgetConfiguration` — 2 new members

**`Body`** — The type of control widget configuration representing the body of this configuration.
```swift
associatedtype Body : ControlWidgetConfiguration
```
**`body`** — The content and behavior of the control.
```swift
@ControlWidgetConfigurationBuilder @MainActor @preconcurrency var body: Self.Body { get }
```

### `ControlWidgetConfigurationBuilder` — 2 new members

**`buildBlock(_:)`** — Passes a single control widget configuration written as a child control through unmodified.  An example of a single cont
```swift
static func buildBlock<Content>(_ content: Content) -> some ControlWidgetConfiguration where Content : ControlWidgetConfiguration
```
**`buildExpression(_:)`** — Builds an expression within the builder.
```swift
static func buildExpression<Content>(_ content: Content) -> Content where Content : ControlWidgetConfiguration
```

### `ControlWidgetTemplate` — 2 new members

**`Body`** — The type of control widget template representing the body of this template.  When you create a custom control widget, Sw
```swift
associatedtype Body : ControlWidgetTemplate
```
**`body`** — The content and behavior of this control widget.
```swift
@ControlWidgetTemplateBuilder @MainActor @preconcurrency var body: Self.Body { get }
```

### `ControlWidgetTemplateBuilder` — 2 new members

**`buildBlock(_:)`** — Passes a single control widget template written as a child view through unmodified.  An example of a single control widg
```swift
static func buildBlock<Content>(_ content: Content) -> some ControlWidgetTemplate where Content : ControlWidgetTemplate
```
**`buildExpression(_:)`** — Builds an expression within the builder.
```swift
static func buildExpression<Content>(_ content: Content) -> Content where Content : ControlWidgetTemplate
```

### `CustomizableToolbarContent` — 1 new members

**`sharedBackgroundVisibility(_:)`** — Controls the visibility of the glass background effect on items in the toolbar. In certain contexts, such as the navigat
```swift
nonisolated func sharedBackgroundVisibility(_ visibility: Visibility) -> some CustomizableToolbarContent
```

### `DatePicker` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `DatePickerStyleConfiguration` — 74 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `DefaultButtonLabel` — 77 new members

**`DefaultButtonLabel.Body`** — The type of view representing the body of this view.  When you create a custom view, Swift infers this type from your im
```swift
typealias Body = some View
```
**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`body`** — The content and behavior of the view.  When you implement a custom view, you must implement a computed `body` property t
```swift
@MainActor @preconcurrency var body: some View { get }
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `DefaultDateProgressLabel` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `DefaultDocumentGroupLaunchActions` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `DefaultSettingsLinkLabel` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `DefaultShareLinkLabel` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `DefaultTabLabel` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `DefaultToolbarItem` — 4 new members

**`DefaultToolbarItem.Body`** — The type of content representing the body of this toolbar content.
```swift
typealias Body = some ToolbarContent
```
**`body`** — The composition of content that comprise the toolbar content.
```swift
@MainActor @preconcurrency var body: some ToolbarContent { get }
```
**`init(kind:placement:)`** — Creates a system-defined toolbar item from a `ToolbarDefaultItemKind` at the given `placement`.  Combinations of `kind` 
```swift
init(kind: ToolbarDefaultItemKind, placement: ToolbarItemPlacement = .automatic)
```
**`sharedBackgroundVisibility(_:)`** — Controls the visibility of the glass background effect on items in the toolbar. In certain contexts, such as the navigat
```swift
nonisolated func sharedBackgroundVisibility(_ visibility: Visibility) -> some ToolbarContent
```

### `DefaultWindowVisibilityToggleLabel` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `DisclosureGroup` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `DisclosureGroupStyleConfiguration` — 148 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `DismissImmersiveSpaceAction` — 1 new members

**`callAsFunction()`** — Dismisses the currently opened immersive space.  Don't call this method directly. SwiftUI calls it when you call the ``E
```swift
@MainActor func callAsFunction() async
```

### `Divider` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `DocumentGroup` — 2 new members

**`defaultLaunchBehavior(_:)`** — Sets the default launch behavior for this scene.  This behavior can be used to define if a scene is shown on application
```swift
nonisolated func defaultLaunchBehavior(_ behavior: SceneLaunchBehavior) -> some Scene
```
**`immersionStyle(selection:in:)`** — Sets the style for an immersive space.  Use this modifier to configure the appearance and behavior of an ``ImmersiveSpac
```swift
nonisolated func immersionStyle(selection: Binding<any ImmersionStyle>, in styles: any ImmersionStyle...) -> some Scene
```

### `DragConfiguration` — 10 new members

**`allowAlias`** — A Boolean value indicating if the drag operation supports creating aliases to the dropped items.
```swift
var allowAlias: Bool { get set }
```
**`allowAlias`** — A Boolean value indicating if the drag operation supports creating aliases to the dropped items.
```swift
var allowAlias: Bool { get set }
```
**`debugDescription`** — A textual representation of this instance, suitable for debugging.  Calling this property directly is discouraged. Inste
```swift
var debugDescription: String { get }
```
**`init(allowCopy:allowMove:allowDelete:)`** — Creates a value that describes the operations allowed for drags that end within the application.
```swift
init(allowCopy: Bool = true, allowMove: Bool = false, allowDelete: Bool = false)
```
**`init(allowCopy:allowMove:allowDelete:)`** — Creates a value that describes the operations allowed for drags to other applications.
```swift
init(allowCopy: Bool = true, allowMove: Bool = false, allowDelete: Bool = false)
```
**`init(allowMove:)`** — Creates a drag configuration that can support drag-to-move in addition to drag-to-copy.  iOS supports drag-to-move opera
```swift
init(allowMove: Bool)
```
**`init(allowMove:allowDelete:)`** — Creates a drag configuration that can support drag-to-move and drag-to-delete in addition to drag-to-copy.
```swift
init(allowMove: Bool, allowDelete: Bool)
```
**`init(operationsWithinApp:operationsOutsideApp:)`** — Creates a default drag configuration with operation `.copy` support for drags within the application and to other applic
```swift
init(operationsWithinApp: DragConfiguration.OperationsWithinApp = .init(), operationsOutsideApp: DragConfiguration.OperationsOutsideApp = .init())
```
**`operationsOutsideApp`** — The operations suggested by the drag source for drags to other applications.
```swift
var operationsOutsideApp: DragConfiguration.OperationsOutsideApp { get set }
```
**`operationsWithinApp`** — The operations suggested by the drag source for drags within the application.
```swift
var operationsWithinApp: DragConfiguration.OperationsWithinApp { get set }
```

### `DragDropPreviewsFormation` — 9 new members

**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: DragDropPreviewsFormation, b: DragDropPreviewsFormation) -> Bool
```
**`default`** — System-determined composition.
```swift
static let `default`: DragDropPreviewsFormation
```
**`description`** — A textual representation of this instance.  Calling this property directly is discouraged. Instead, convert an instance 
```swift
var description: String { get }
```
**`hash(into:)`** — Hashes the essential components of this value by feeding them into the given hasher.  Implement this method to conform t
```swift
func hash(into hasher: inout Hasher)
```
**`hashValue`** — The hash value.  Hash values are not guaranteed to be equal across different executions of your program. Do not save has
```swift
var hashValue: Int { get }
```
**`list`** — Drag images are laid out vertically, non-overlapping, and the left edges are aligned.
```swift
static let list: DragDropPreviewsFormation
```
**`none`** — Drag images maintain their set positions relative to each other.
```swift
static let none: DragDropPreviewsFormation
```
**`pile`** — Drag images are placed on top of each other with random rotations.
```swift
static let pile: DragDropPreviewsFormation
```
**`stack`** — Drag images are laid out overlapping diagonally.
```swift
static let stack: DragDropPreviewsFormation
```

### `DragSession` — 18 new members

**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: DragSession.Phase, b: DragSession.Phase) -> Bool
```
**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: DragSession.ID, b: DragSession.ID) -> Bool
```
**`DragSession.Phase.active`** — The drag has moved to a new location.
```swift
case active
```
**`DragSession.Phase.dataTransferCompleted`** — Dragged items have been transferred. You can remove temporary items, perform any cleanup if needed.
```swift
case dataTransferCompleted
```
**`DragSession.Phase.ended(_:)`** — The drag has ended.
```swift
case ended(DropOperation)
```
**`DragSession.Phase.initial`** — The drag session is about to begin
```swift
case initial
```
**`debugDescription`** — A textual representation of this instance, suitable for debugging.  Calling this property directly is discouraged. Inste
```swift
var debugDescription: String { get }
```
**`description`** — A textual representation of this instance.  Calling this property directly is discouraged. Instead, convert an instance 
```swift
var description: String { get }
```
**`draggedItemIDs(for:)`** — Provides an array of identifiers of the currently dragged items in a case when the items conform to the `Identifiable` p
```swift
func draggedItemIDs<ItemID>(for type: ItemID.Type) -> [ItemID] where ItemID : Hashable
```
**`draggedItemIndex`** — The index of the dragged item under the cursor.
```swift
var draggedItemIndex: Int { get }
```
**`hash(into:)`** — Hashes the essential components of this value by feeding them into the given hasher.  Implement this method to conform t
```swift
func hash(into hasher: inout Hasher)
```
**`hash(into:)`** — Hashes the essential components of this value by feeding them into the given hasher.  Implement this method to conform t
```swift
func hash(into hasher: inout Hasher)
```
**`hashValue`** — The hash value.  Hash values are not guaranteed to be equal across different executions of your program. Do not save has
```swift
var hashValue: Int { get }
```
**`hashValue`** — The hash value.  Hash values are not guaranteed to be equal across different executions of your program. Do not save has
```swift
var hashValue: Int { get }
```
**`id`** — The identifier of the drag session.
```swift
var id: DragSession.ID { get }
```
**`location`** — Location of the drag session in the local coordinate space.
```swift
var location: CGPoint
```
**`matches(_:)`** — Checks if the session value describes the same drag session as the object provided by AppKit.
```swift
func matches(_ dragSession: NSDraggingSession) -> Bool
```
**`phase`** — The current phase of the drag session.
```swift
var phase: DragSession.Phase { get }
```

### `DropConfiguration` — 3 new members

**`acceptedItemCount`** — Specifies the number of items that the drop side wants to accept.  Some drop destinations can accept only a limited numb
```swift
var acceptedItemCount: Int? { get set }
```
**`init(operation:)`** — Creates a configuration value with the operation chosen by the drop destination.  Parameter operation: The drop operatio
```swift
init(operation: DropOperation)
```
**`operation`** — The drop operation that the drop chooses to perform.
```swift
var operation: DropOperation { get }
```

### `DropOperation` — 14 new members

**`DropOperation.Set.ArrayLiteralElement`** — The type of the elements of an array literal.
```swift
typealias ArrayLiteralElement = DropOperation.Set
```
**`DropOperation.Set.Element`** — The element type of the option set.  To inherit all the default implementations from the `OptionSet` protocol, the `Elem
```swift
typealias Element = DropOperation.Set
```
**`DropOperation.Set.RawValue`** — The raw type that can be used to represent all values of the conforming type.  Every distinct value of the conforming ty
```swift
typealias RawValue = Int
```
**`DropOperation.alias`**
```swift
case alias
```
**`DropOperation.delete`** — Delete the data. The item was dragged to Trash or to another destination that semantically represents deletion.
```swift
case delete
```
**`alias`**
```swift
static let alias: DropOperation.Set
```
**`cancel`** — Cancel the operation.
```swift
static let cancel: DropOperation.Set
```
**`copy`** — Copy the data to the modified view.
```swift
static let copy: DropOperation.Set
```
**`delete`** — Delete the data.
```swift
static let delete: DropOperation.Set
```
**`description`** — A textual representation of this instance.  Calling this property directly is discouraged. Instead, convert an instance 
```swift
var description: String { get }
```
**`forbidden`** — The operation is forbidden.
```swift
static let forbidden: DropOperation.Set
```
**`init(rawValue:)`** — Creates a set of drop operations using the provided raw value.
```swift
init(rawValue: Int)
```
**`move`** — Move the data represented by the drag items instead of copying it.
```swift
static let move: DropOperation.Set
```
**`rawValue`** — The corresponding value of the raw type.  A new instance initialized with `rawValue` will be equivalent to this instance
```swift
let rawValue: Int
```

### `DropProposal` — 3 new members

**`debugDescription`** — A textual representation of this instance, suitable for debugging.  Calling this property directly is discouraged. Inste
```swift
var debugDescription: String { get }
```
**`init(withinApplication:outsideApplication:)`**
```swift
init(withinApplication: DropOperation, outsideApplication: DropOperation)
```
**`operationOutsideApplication`** — The drop operation for drops outside the source application.
```swift
let operationOutsideApplication: DropOperation?
```

### `DropSession` — 23 new members

**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: DropSession.ID, b: DropSession.ID) -> Bool
```
**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (lhs: DropSession.Phase, rhs: DropSession.Phase) -> Bool
```
**`DropSession.Phase.active`** — The drop session is active inside the drop destination.
```swift
case active
```
**`DropSession.Phase.dataTransferCompleted`** — Dragged items have been transferred. You can remove temporary items, perform any cleanup if needed.
```swift
case dataTransferCompleted
```
**`DropSession.Phase.ended(_:)`** — The drop has ended.
```swift
case ended(DropOperation)
```
**`DropSession.Phase.entering`** — The drop session is entering the drop destination.
```swift
case entering
```
**`DropSession.Phase.exiting`** — The drop session has exited the drop destination.
```swift
case exiting
```
**`debugDescription`** — A textual representation of this instance, suitable for debugging.  Calling this property directly is discouraged. Inste
```swift
var debugDescription: String { get }
```
**`description`** — A textual representation of this instance.  Calling this property directly is discouraged. Instead, convert an instance 
```swift
var description: String { get }
```
**`description`** — A textual representation of this instance.  Calling this property directly is discouraged. Instead, convert an instance 
```swift
var description: String { get }
```
**`draggedItemIDs(for:)`** — Provides an array of identifiers of the currently dragged items if available.  If drag started within the application, t
```swift
func draggedItemIDs<ItemID>(for type: ItemID.Type) -> [ItemID] where ItemID : Hashable
```
**`hash(into:)`** — Hashes the essential components of this value by feeding them into the given hasher.  Implement this method to conform t
```swift
func hash(into hasher: inout Hasher)
```
**`hash(into:)`** — Hashes the essential components of this value by feeding them into the given hasher.  Implement this method to conform t
```swift
func hash(into hasher: inout Hasher)
```
**`hashValue`** — The hash value.  Hash values are not guaranteed to be equal across different executions of your program. Do not save has
```swift
var hashValue: Int { get }
```
**`hashValue`** — The hash value.  Hash values are not guaranteed to be equal across different executions of your program. Do not save has
```swift
var hashValue: Int { get }
```
**`id`** — The unique identifier of the drop session.
```swift
var id: DropSession.ID { get }
```
**`itemsCount`** — Number of items for the drop.
```swift
var itemsCount: Int
```
**`localSession`** — Provides additional information about a session if it originated within the app.  This property is set to `nil` if drag 
```swift
var localSession: DropSession.LocalSession?
```
**`location`** — Location of drop in the local coordinate space
```swift
var location: CGPoint
```
**`matches(_:)`** — Checks if the session value describes the same drag session as the object provided by AppKit.
```swift
func matches(_ dragInfo: any NSDraggingInfo) -> Bool
```
**`phase`** — The phase of the current drop session.
```swift
var phase: DropSession.Phase { get }
```
**`size`** — Size of the drop destination view.
```swift
var size: CGSize
```
**`suggestedOperations`** — Operations suggested by the drag source.
```swift
var suggestedOperations: DropOperation.Set
```

### `EditableCollectionContent` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `EmptyControlWidgetConfiguration` — 2 new members

**`EmptyControlWidgetConfiguration.Body`** — The type of control widget configuration representing the body of this configuration.
```swift
typealias Body = Never
```
**`init()`**
```swift
@MainActor @preconcurrency init()
```

### `EmptyControlWidgetTemplate` — 2 new members

**`EmptyControlWidgetTemplate.Body`** — The type of control widget template representing the body of this template.  When you create a custom control widget, Sw
```swift
typealias Body = Never
```
**`init()`**
```swift
@MainActor @preconcurrency init()
```

### `EquatableView` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `FindContext` — 2 new members

**`isPresented`** — A binding controlling if the find navigator is presented, or nil if no binding has been provided via the ``View/findNavi
```swift
var isPresented: Binding<Bool>?
```
**`supportsReplace`** — If the find navigators in this context should support replacing.
```swift
var supportsReplace: Bool
```

### `Form` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `FormStyleConfiguration` — 74 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `FullImmersionStyle` — 2 new members

**`full`** — An immersion style that displays unbounded content that completely replaces passthrough video.  When this immersion styl
```swift
static var full: FullImmersionStyle { get }
```
**`init()`**
```swift
init()
```

### `Gauge` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `GaugeStyleConfiguration` — 370 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `GlassButtonStyle` — 6 new members

**`GlassButtonStyle.Body`** — A view that represents the body of a button.
```swift
typealias Body = some View
```
**`glass`** — A button style that applies glass border artwork based on the button's context.  To apply this style to a button, or to 
```swift
@MainActor @preconcurrency static var glass: GlassButtonStyle { get }
```
**`glass(_:)`** — A button style that applies a configurable glass border artwork based on the button's context.  This style can be config
```swift
@MainActor @preconcurrency static func glass(_ glass: Glass) -> Self
```
**`init()`** — Creates a glass button style.
```swift
init()
```
**`init(_:)`**
```swift
init(_ glass: Glass)
```
**`makeBody(configuration:)`** — Creates a view that represents the body of a button.  The system calls this method for each ``Button`` instance in a vie
```swift
@MainActor @preconcurrency func makeBody(configuration: GlassButtonStyle.Configuration) -> some View
```

### `GlassProminentButtonStyle` — 4 new members

**`GlassProminentButtonStyle.Body`** — A view that represents the body of a button.
```swift
typealias Body = some View
```
**`glassProminent`** — A button style that applies prominent glass border artwork based on the button's context.  To apply this style to a butt
```swift
@MainActor @preconcurrency static var glassProminent: GlassProminentButtonStyle { get }
```
**`init()`** — Creates a prominent glass button style.
```swift
init()
```
**`makeBody(configuration:)`** — Creates a view that represents the body of a button.  The system calls this method for each ``Button`` instance in a vie
```swift
@MainActor @preconcurrency func makeBody(configuration: GlassProminentButtonStyle.Configuration) -> some View
```

### `Grid` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `GridItem` — 2 new members

**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: GridItem.Size, b: GridItem.Size) -> Bool
```
**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: GridItem, b: GridItem) -> Bool
```

### `GridRow` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `GroupBox` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `GroupBoxStyleConfiguration` — 148 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `HSplitView` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `HelpLink` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `ImmersionChangeContext` — 1 new members

**`amount`** — The current amount of immersion.  Your app can display virtual content using ``ImmersiveSpace`` and the different immers
```swift
let amount: Double?
```

### `ImmersionStyle` — 10 new members

**`automatic`** — The default immersion style.  The system uses this style for an ``ImmersiveSpace`` if you don't provide an ``Scene/immer
```swift
static var automatic: AutomaticImmersionStyle { get }
```
**`full`** — An immersion style that displays unbounded content that completely replaces passthrough video.  When this immersion styl
```swift
static var full: FullImmersionStyle { get }
```
**`progressive`** — An immersion style that displays unbounded content that partially replaces passthrough video.  The system initially uses
```swift
static var progressive: ProgressiveImmersionStyle { get }
```
**`progressive(_:initialAmount:)`** — An immersion style that displays unbounded content that partially replaces passthrough video.  The immersion style affec
```swift
static func progressive(_ immersionRange: PartialRangeFrom<Double>, initialAmount: Double? = nil) -> ProgressiveImmersionStyle
```
**`progressive(_:initialAmount:)`** — An immersion style that displays unbounded content that partially replaces passthrough video.  The immersion style affec
```swift
static func progressive(_ immersionRange: PartialRangeThrough<Double>, initialAmount: Double? = nil) -> ProgressiveImmersionStyle
```
**`progressive(_:initialAmount:)`** — An immersion style that displays unbounded content that partially replaces passthrough video.  The immersion style affec
```swift
static func progressive(_ immersionRange: ClosedRange<Double>, initialAmount: Double? = nil) -> ProgressiveImmersionStyle
```
**`progressive(_:initialAmount:aspectRatio:)`** — An immersion style that displays unbounded content that partially replaces passthrough video.  Use the ``Scene/immersion
```swift
static func progressive(_ immersionRange: PartialRangeFrom<Double>, initialAmount: Double? = nil, aspectRatio: ProgressiveImmersionAspectRatio?) -> ProgressiveImmersionStyle
```
**`progressive(_:initialAmount:aspectRatio:)`** — An immersion style that displays unbounded content that partially replaces passthrough video.  Use the ``Scene/immersion
```swift
static func progressive(_ immersionRange: PartialRangeThrough<Double>, initialAmount: Double? = nil, aspectRatio: ProgressiveImmersionAspectRatio?) -> ProgressiveImmersionStyle
```
**`progressive(_:initialAmount:aspectRatio:)`** — An immersion style that displays unbounded content that partially replaces passthrough video.  Use the ``Scene/immersion
```swift
static func progressive(_ immersionRange: ClosedRange<Double>, initialAmount: Double? = nil, aspectRatio: ProgressiveImmersionAspectRatio?) -> ProgressiveImmersionStyle
```
**`progressive(aspectRatio:)`** — An immersion style that displays unbounded content that partially replaces passthrough video.  Use the ``Scene/immersion
```swift
static func progressive(aspectRatio: ProgressiveImmersionAspectRatio) -> ProgressiveImmersionStyle
```

### `ImmersiveSpaceContent` — 2 new members

**`Body`**
```swift
associatedtype Body : ImmersiveSpaceContent
```
**`body`**
```swift
@ImmersiveSpaceContentBuilder @MainActor @preconcurrency var body: Self.Body { get }
```

### `ImmersiveSpaceContentBuilder` — 1 new members

**`buildBlock(_:)`**
```swift
static func buildBlock<Content>(_ content: Content) -> Content where Content : ImmersiveSpaceContent
```

### `Label` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `LabelStyleConfiguration` — 148 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `LabeledContent` — 78 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`init(_:content:)`** — Creates a labeled view that generates its label from a localized string resource.  This initializer creates a ``Text`` l
```swift
init(_ titleResource: LocalizedStringResource, @ViewBuilder content: () -> Content)
```
**`init(_:value:)`** — Creates a labeled informational view.  This initializer creates a ``Text`` label on your behalf. See `Text` for more inf
```swift
init<S>(_ titleResource: LocalizedStringResource, value: S) where S : StringProtocol
```
**`init(_:value:format:)`** — Creates a labeled informational view from a formatted value.  This initializer creates a ``Text`` label on your behalf. 
```swift
init<F>(_ titleResource: LocalizedStringResource, value: F.FormatInput, format: F) where F : FormatStyle, F.FormatInput : Equatable, F.FormatOutput == String
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `LabeledContentStyleConfiguration` — 148 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `LabeledControlGroupContent` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `LabeledToolbarItemGroupContent` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `LazyHGrid` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `LazyVGrid` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `Link` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `List` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `Menu` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `MenuBarExtra` — 2 new members

**`defaultLaunchBehavior(_:)`** — Sets the default launch behavior for this scene.  This behavior can be used to define if a scene is shown on application
```swift
nonisolated func defaultLaunchBehavior(_ behavior: SceneLaunchBehavior) -> some Scene
```
**`immersionStyle(selection:in:)`** — Sets the style for an immersive space.  Use this modifier to configure the appearance and behavior of an ``ImmersiveSpac
```swift
nonisolated func immersionStyle(selection: Binding<any ImmersionStyle>, in styles: any ImmersionStyle...) -> some Scene
```

### `MenuButton` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `MenuStyleConfiguration` — 148 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `NSGestureRecognizerRepresentable` — 11 new members

**`Coordinator`** — A type to coordinate with the gesture recognizer.
```swift
associatedtype Coordinator = Void
```
**`NSGestureRecognizerRepresentable.Context`** — Contextual information about the state of the system that you use to create and update your gesture recognizer.
```swift
typealias Context = NSGestureRecognizerRepresentableContext<Self>
```
**`NSGestureRecognizerRepresentable.CoordinateSpaceConverter`** — A type used to convert coordinates to/from coordinate spaces in the hierarchy of the associated SwiftUI view.
```swift
typealias CoordinateSpaceConverter = NSGestureRecognizerRepresentableCoordinateSpaceConverter
```
**`NSGestureRecognizerType`** — The type of `NSGestureRecognizer` to be presented.
```swift
associatedtype NSGestureRecognizerType : NSGestureRecognizer
```
**`handleNSGestureRecognizerAction(_:context:)`** — Handles recognition of the represented `NSGestureRecognizer`.  If you implement this method, SwiftUI calls it when the w
```swift
@MainActor @preconcurrency func handleNSGestureRecognizerAction(_ recognizer: Self.NSGestureRecognizerType, context: Self.Context)
```
**`handleNSGestureRecognizerAction(_:context:)`** — Handles recognition of the represented `NSGestureRecognizer`.  If you implement this method, SwiftUI calls it when the w
```swift
@MainActor @preconcurrency func handleNSGestureRecognizerAction(_ recognizer: Self.NSGestureRecognizerType, context: Self.Context)
```
**`makeCoordinator(converter:)`** — Creates the custom object that you use to communicate state changes from your gesture recognizer to other parts of your 
```swift
@MainActor @preconcurrency func makeCoordinator(converter: Self.CoordinateSpaceConverter) -> Self.Coordinator
```
**`makeCoordinator(converter:)`**
```swift
@MainActor @preconcurrency func makeCoordinator(converter: Self.CoordinateSpaceConverter)
```
**`makeNSGestureRecognizer(context:)`** — Creates an instance of the represented gesture recognizer.  - Parameter context: A context structure containing informat
```swift
@MainActor @preconcurrency func makeNSGestureRecognizer(context: Self.Context) -> Self.NSGestureRecognizerType
```
**`updateNSGestureRecognizer(_:context:)`** — Updates the `NSGestureRecognizer` (and coordinator) to the latest configuration.  - Parameter recognizer: An instance of
```swift
@MainActor @preconcurrency func updateNSGestureRecognizer(_ recognizer: Self.NSGestureRecognizerType, context: Self.Context)
```
**`updateNSGestureRecognizer(_:context:)`** — Updates the `NSGestureRecognizer` (and coordinator) to the latest configuration.  - Parameter recognizer: An instance of
```swift
@MainActor @preconcurrency func updateNSGestureRecognizer(_ recognizer: Self.NSGestureRecognizerType, context: Self.Context)
```

### `NSGestureRecognizerRepresentableContext` — 2 new members

**`converter`** — A structure used to convert locations to and from coordinate spaces in the hierarchy of the associated SwiftUI view.
```swift
let converter: NSGestureRecognizerRepresentableCoordinateSpaceConverter
```
**`coordinator`** — The custom object that you use to communicate state changes from your gesture recognizer to other parts of your SwiftUI 
```swift
let coordinator: Representable.Coordinator
```

### `NSGestureRecognizerRepresentableCoordinateSpaceConverter` — 7 new members

**`convert(globalPoint:to:)`** — Converts a point in the global coordinate space to a SwiftUI coordinate space of an ancestor of the view the gesture rec
```swift
func convert(globalPoint: CGPoint, to coordinateSpace: some CoordinateSpaceProtocol = .local) -> CGPoint
```
**`localLocation`** — The represented gesture recognizer's current location in the coordinate space of the SwiftUI view it's attached to.
```swift
var localLocation: CGPoint { get }
```
**`localTranslation`** — The represented gesture recognizer's current translation in the coordinate space of the SwiftUI view it's attached to, o
```swift
var localTranslation: CGPoint? { get }
```
**`localVelocity`** — The represented gesture recognizer's current velocity in the coordinate space of the SwiftUI view it's attached to, or `
```swift
var localVelocity: CGPoint? { get }
```
**`location(in:)`** — Converts the represented gesture recognizer's current location to a SwiftUI coordinate space of an ancestor of the view 
```swift
func location(in coordinateSpace: some CoordinateSpaceProtocol) -> CGPoint
```
**`translation(in:)`** — Converts the represented gesture recognizer's current translation to a SwiftUI coordinate space of an ancestor of the vi
```swift
func translation(in coordinateSpace: some CoordinateSpaceProtocol) -> CGPoint?
```
**`velocity(in:)`** — Converts the represented gesture recognizer's current velocity to a SwiftUI coordinate space of an ancestor of the view 
```swift
func velocity(in coordinateSpace: some CoordinateSpaceProtocol) -> CGPoint?
```

### `NSHostingSceneRepresentation` — 2 new members

**`environment`** — The environment for any scene(s) being represented by `self`.  Use the environment values to programmatically present a 
```swift
@MainActor var environment: EnvironmentValues { get }
```
**`init(rootScene:)`** — Creates a new hosting scene representation for the specified scene(s).  - Parameter rootScene: The SwiftUI scene(s) to b
```swift
@MainActor init(@SceneBuilder rootScene: () -> Content)
```

### `NavigationLink` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `NavigationSplitView` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `NavigationStack` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `NavigationView` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `NewDocumentButton` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `OpenImmersiveSpaceAction` — 9 new members

**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: OpenImmersiveSpaceAction.Result, b: OpenImmersiveSpaceAction.Result) -> Bool
```
**`OpenImmersiveSpaceAction.Result.error`** — The system was unable to open the immersive space.
```swift
case error
```
**`OpenImmersiveSpaceAction.Result.opened`** — The immersive space opened.
```swift
case opened
```
**`OpenImmersiveSpaceAction.Result.userCancelled`** — The immersive space didn't open because the user cancelled the operation.
```swift
case userCancelled
```
**`callAsFunction(id:)`** — Presents an immersive space for the scene with the specified identifier.  Don't call this method directly. SwiftUI calls
```swift
@discardableResult @MainActor func callAsFunction(id: String) async -> OpenImmersiveSpaceAction.Result
```
**`callAsFunction(id:value:)`** — Presents the immersive space that your app defines for the specified identifier and that handles the type of the present
```swift
@discardableResult @MainActor func callAsFunction<D>(id: String, value: D) async -> OpenImmersiveSpaceAction.Result where D : Decodable, D : Encodable, D : Hashable
```
**`callAsFunction(value:)`** — Presents the immersive space that handles the type of the presented value.  Don't call this method directly. SwiftUI cal
```swift
@discardableResult @MainActor func callAsFunction<D>(value: D) async -> OpenImmersiveSpaceAction.Result where D : Decodable, D : Encodable, D : Hashable
```
**`hash(into:)`** — Hashes the essential components of this value by feeding them into the given hasher.  Implement this method to conform t
```swift
func hash(into hasher: inout Hasher)
```
**`hashValue`** — The hash value.  Hash values are not guaranteed to be equal across different executions of your program. Do not save has
```swift
var hashValue: Int { get }
```

### `OutlineGroup` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `OutlineSubgroupChildren` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `PasteButton` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `PencilDoubleTapGestureValue` — 4 new members

**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: PencilDoubleTapGestureValue, b: PencilDoubleTapGestureValue) -> Bool
```
**`hash(into:)`** — Hashes the essential components of this value by feeding them into the given hasher.  Implement this method to conform t
```swift
func hash(into hasher: inout Hasher)
```
**`hashValue`** — The hash value.  Hash values are not guaranteed to be equal across different executions of your program. Do not save has
```swift
var hashValue: Int { get }
```
**`hoverPose`** — The location and distance of an Apple Pencil hovering in the area above the view’s bounds when the double-tap gesture oc
```swift
let hoverPose: PencilHoverPose?
```

### `PencilHoverPose` — 9 new members

**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: PencilHoverPose, b: PencilHoverPose) -> Bool
```
**`altitude`** — A value that represents the altitude angle of the hovering Apple Pencil.  This value is `.zero` when the Apple Pencil is
```swift
let altitude: Angle
```
**`anchor`** — The location of an Apple Pencil hovering in the area above the view’s bounds, expressed as a normalized anchor point rel
```swift
let anchor: UnitPoint
```
**`azimuth`** — A value that represents the azimuth angle of a hovering Apple Pencil.  This value is `.zero` when the Apple Pencil point
```swift
let azimuth: Angle
```
**`hash(into:)`** — Hashes the essential components of this value by feeding them into the given hasher.  Implement this method to conform t
```swift
func hash(into hasher: inout Hasher)
```
**`hashValue`** — The hash value.  Hash values are not guaranteed to be equal across different executions of your program. Do not save has
```swift
var hashValue: Int { get }
```
**`location`** — The location of an Apple Pencil hovering in the area above the view’s bounds, expressed as a point in that view’s coordi
```swift
let location: CGPoint
```
**`roll`** — A value that represents the barrel roll angle of the hovering Apple Pencil.  This value is `.zero` when the user starts 
```swift
let roll: Angle
```
**`zDistance`** — The normalized distance between the screen and a hovering Apple Pencil.  This value is `1` at the maximum distance from 
```swift
let zDistance: CGFloat
```

### `PencilPreferredAction` — 10 new members

**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: PencilPreferredAction, b: PencilPreferredAction) -> Bool
```
**`hash(into:)`** — Hashes the essential components of this value by feeding them into the given hasher.  Implement this method to conform t
```swift
func hash(into hasher: inout Hasher)
```
**`hashValue`** — The hash value.  Hash values are not guaranteed to be equal across different executions of your program. Do not save has
```swift
var hashValue: Int { get }
```
**`ignore`** — An action that does nothing.
```swift
static let ignore: PencilPreferredAction
```
**`runSystemShortcut`** — An action that runs a system shortcut.  If the user selects this as their preferred action to perform after double-tappi
```swift
static let runSystemShortcut: PencilPreferredAction
```
**`showColorPalette`** — An action that toggles the display of the color palette.
```swift
static let showColorPalette: PencilPreferredAction
```
**`showContextualPalette`** — An action that toggles the display of the contextual palette, or the undo/redo panel if contextual palette is not availa
```swift
static let showContextualPalette: PencilPreferredAction
```
**`showInkAttributes`** — An action that toggles the display of the current tool’s ink attributes.
```swift
static let showInkAttributes: PencilPreferredAction
```
**`switchEraser`** — An action that switches between the current tool and the eraser.
```swift
static let switchEraser: PencilPreferredAction
```
**`switchPrevious`** — An action that switches between the current tool and the last used tool.
```swift
static let switchPrevious: PencilPreferredAction
```

### `PencilSqueezeGesturePhase` — 4 new members

**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: PencilSqueezeGesturePhase, b: PencilSqueezeGesturePhase) -> Bool
```
**`PencilSqueezeGesturePhase.active(_:)`** — The user started squeezing their Apple Pencil.
```swift
case active(PencilSqueezeGestureValue)
```
**`PencilSqueezeGesturePhase.ended(_:)`** — The user successfully completed a squeeze gesture.
```swift
case ended(PencilSqueezeGestureValue)
```
**`PencilSqueezeGesturePhase.failed`** — The user started squeezing their Apple Pencil but failed to successfully complete the gesture.
```swift
case failed
```

### `PencilSqueezeGestureValue` — 4 new members

**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: PencilSqueezeGestureValue, b: PencilSqueezeGestureValue) -> Bool
```
**`hash(into:)`** — Hashes the essential components of this value by feeding them into the given hasher.  Implement this method to conform t
```swift
func hash(into hasher: inout Hasher)
```
**`hashValue`** — The hash value.  Hash values are not guaranteed to be equal across different executions of your program. Do not save has
```swift
var hashValue: Int { get }
```
**`hoverPose`** — The location and distance of an Apple Pencil hovering in the area above the view’s bounds when the squeeze gesture occur
```swift
let hoverPose: PencilHoverPose?
```

### `Picker` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `PresentedWindowContent` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `PreviewModifierContent` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `PrimitiveButtonStyle` — 3 new members

**`glass`** — A button style that applies glass border artwork based on the button's context.  To apply this style to a button, or to 
```swift
@MainActor @preconcurrency static var glass: GlassButtonStyle { get }
```
**`glass(_:)`** — A button style that applies a configurable glass border artwork based on the button's context.  This style can be config
```swift
@MainActor @preconcurrency static func glass(_ glass: Glass) -> Self
```
**`glassProminent`** — A button style that applies prominent glass border artwork based on the button's context.  To apply this style to a butt
```swift
@MainActor @preconcurrency static var glassProminent: GlassProminentButtonStyle { get }
```

### `PrimitiveButtonStyleConfiguration` — 74 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `ProgressView` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `ProgressViewStyleConfiguration` — 148 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `ProgressiveImmersionAspectRatio` — 4 new members

**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: ProgressiveImmersionAspectRatio, b: ProgressiveImmersionAspectRatio) -> Bool
```
**`automatic`** — The system will choose a default portal aspect ratio.
```swift
static var automatic: ProgressiveImmersionAspectRatio { get }
```
**`landscape`** — The portal will be wide.
```swift
static var landscape: ProgressiveImmersionAspectRatio { get }
```
**`portrait`** — The portal will be tall.
```swift
static var portrait: ProgressiveImmersionAspectRatio { get }
```

### `ProgressiveImmersionStyle` — 16 new members

**`aspectRatio`** — The aspect ratio used for this instance of the style.  The value controls the aspect ratio of the ImmersiveSpace portal.
```swift
let aspectRatio: ProgressiveImmersionAspectRatio
```
**`init()`** — An immersion style that displays unbounded content that partially replaces passthrough video.  The system initially uses
```swift
init()
```
**`init(immersion:initialAmount:)`** — An immersion style that displays unbounded content that partially replaces passthrough video.  The immersion style affec
```swift
init(immersion: PartialRangeFrom<Double>, initialAmount: Double? = nil)
```
**`init(immersion:initialAmount:)`** — An immersion style that displays unbounded content that partially replaces passthrough video.  The immersion style affec
```swift
init(immersion: PartialRangeThrough<Double>, initialAmount: Double? = nil)
```
**`init(immersion:initialAmount:)`** — An immersion style that displays unbounded content that partially replaces passthrough video.  The immersion style affec
```swift
init(immersion: ClosedRange<Double>, initialAmount: Double? = nil)
```
**`initialImmersionAmount`** — The initial amount of immersion used for this instance of the style.  The value represents how much of the spherical fie
```swift
let initialImmersionAmount: Double?
```
**`maximumImmersionAmount`** — The maximum amount of immersion used for this instance of the style.  The value represents the maximum amount of the sph
```swift
let maximumImmersionAmount: Double?
```
**`minimumImmersionAmount`** — The minimum amount of immersion used for this instance of the style.  The value represents the minimum amount of the sph
```swift
let minimumImmersionAmount: Double?
```
**`progressive`** — An immersion style that displays unbounded content that partially replaces passthrough video.  The system initially uses
```swift
static var progressive: ProgressiveImmersionStyle { get }
```
**`progressive(_:initialAmount:)`** — An immersion style that displays unbounded content that partially replaces passthrough video.  The immersion style affec
```swift
static func progressive(_ immersionRange: PartialRangeFrom<Double>, initialAmount: Double? = nil) -> ProgressiveImmersionStyle
```
**`progressive(_:initialAmount:)`** — An immersion style that displays unbounded content that partially replaces passthrough video.  The immersion style affec
```swift
static func progressive(_ immersionRange: PartialRangeThrough<Double>, initialAmount: Double? = nil) -> ProgressiveImmersionStyle
```
**`progressive(_:initialAmount:)`** — An immersion style that displays unbounded content that partially replaces passthrough video.  The immersion style affec
```swift
static func progressive(_ immersionRange: ClosedRange<Double>, initialAmount: Double? = nil) -> ProgressiveImmersionStyle
```
**`progressive(_:initialAmount:aspectRatio:)`** — An immersion style that displays unbounded content that partially replaces passthrough video.  Use the ``Scene/immersion
```swift
static func progressive(_ immersionRange: PartialRangeFrom<Double>, initialAmount: Double? = nil, aspectRatio: ProgressiveImmersionAspectRatio?) -> ProgressiveImmersionStyle
```
**`progressive(_:initialAmount:aspectRatio:)`** — An immersion style that displays unbounded content that partially replaces passthrough video.  Use the ``Scene/immersion
```swift
static func progressive(_ immersionRange: PartialRangeThrough<Double>, initialAmount: Double? = nil, aspectRatio: ProgressiveImmersionAspectRatio?) -> ProgressiveImmersionStyle
```
**`progressive(_:initialAmount:aspectRatio:)`** — An immersion style that displays unbounded content that partially replaces passthrough video.  Use the ``Scene/immersion
```swift
static func progressive(_ immersionRange: ClosedRange<Double>, initialAmount: Double? = nil, aspectRatio: ProgressiveImmersionAspectRatio?) -> ProgressiveImmersionStyle
```
**`progressive(aspectRatio:)`** — An immersion style that displays unbounded content that partially replaces passthrough video.  Use the ``Scene/immersion
```swift
static func progressive(aspectRatio: ProgressiveImmersionAspectRatio) -> ProgressiveImmersionStyle
```

### `RemoteDeviceIdentifier` — 3 new members

**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (lhs: RemoteDeviceIdentifier, rhs: RemoteDeviceIdentifier) -> Bool
```
**`hash(into:)`** — Hashes the essential components of this value by feeding them into the given hasher.  Implement this method to conform t
```swift
func hash(into hasher: inout Hasher)
```
**`hashValue`** — The hash value.  Hash values are not guaranteed to be equal across different executions of your program. Do not save has
```swift
var hashValue: Int { get }
```

### `RemoteImmersiveSpace` — 10 new members

**`RemoteImmersiveSpace.Body`** — The type of scene that represents the body of this scene.  When you create a custom scene, Swift infers this type from y
```swift
typealias Body = some Scene
```
**`body`** — The content and behavior of the scene.  For any scene that you create, provide a computed `body` property that defines t
```swift
@MainActor @preconcurrency var body: some Scene { get }
```
**`defaultLaunchBehavior(_:)`** — Sets the default launch behavior for this scene.  This behavior can be used to define if a scene is shown on application
```swift
nonisolated func defaultLaunchBehavior(_ behavior: SceneLaunchBehavior) -> some Scene
```
**`immersionStyle(selection:in:)`** — Sets the style for an immersive space.  Use this modifier to configure the appearance and behavior of an ``ImmersiveSpac
```swift
nonisolated func immersionStyle(selection: Binding<any ImmersionStyle>, in styles: any ImmersionStyle...) -> some Scene
```
**`init(content:)`** — Creates a remote immersive space.  The space uses the specified content builder to form the content.  - Parameters:   - 
```swift
nonisolated init<C>(@CompositorContentBuilder content: @escaping () -> C) where Content == CompositorContentBuilder.Content<C>, Data == Never, C : CompositorContent
```
**`init(for:content:)`** — Creates the remote immersive space for a specified type of presented data.  The space uses the specified content builder
```swift
nonisolated init<C>(for type: Data.Type, @CompositorContentBuilder content: @escaping (Binding<Data?>) -> C) where Content == CompositorContentBuilder.Content<C>, C : CompositorContent
```
**`init(for:content:defaultValue:)`** — Creates the remote immersive space for a specified type of presented data, and a default value, if the data is not set. 
**`init(id:content:)`** — Creates the remote immersive space associated with the specified identifier.  The space uses the specified content build
```swift
nonisolated init<C>(id: String, @CompositorContentBuilder content: @escaping () -> C) where Content == CompositorContentBuilder.Content<C>, Data == Never, C : CompositorContent
```
**`init(id:for:content:)`** — Creates the remote immersive space associated with an identifier for a specified type of presented data.  The space uses
```swift
nonisolated init<C>(id: String, for type: Data.Type, @CompositorContentBuilder content: @escaping (Binding<Data?>) -> C) where Content == CompositorContentBuilder.Content<C>, C : CompositorContent
```
**`init(id:for:content:defaultValue:)`** — Creates the remote immersive space associated with an identifier for a specified type of presented data, and a default v

### `RenameButton` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `Scene` — 2 new members

**`defaultLaunchBehavior(_:)`** — Sets the default launch behavior for this scene.  This behavior can be used to define if a scene is shown on application
```swift
nonisolated func defaultLaunchBehavior(_ behavior: SceneLaunchBehavior) -> some Scene
```
**`immersionStyle(selection:in:)`** — Sets the style for an immersive space.  Use this modifier to configure the appearance and behavior of an ``ImmersiveSpac
```swift
nonisolated func immersionStyle(selection: Binding<any ImmersionStyle>, in styles: any ImmersionStyle...) -> some Scene
```

### `SceneLaunchBehavior` — 3 new members

**`automatic`** — The automatic behavior.  A scene with the automatic behavior will present itself on launch if it is the first scene defi
```swift
static let automatic: SceneLaunchBehavior
```
**`presented`** — The presented behavior. The scene will present itself in the absence of any previously restored scenes.
```swift
static let presented: SceneLaunchBehavior
```
**`suppressed`** — The suppressed behavior. The scene will not present itself in the absence of any previously restored scenes.
```swift
static let suppressed: SceneLaunchBehavior
```

### `SceneRestorationBehavior` — 1 new members

**`disabled`** — The disabled behavior. The scene's windows will not be restored.
```swift
static let disabled: SceneRestorationBehavior
```

### `ScrollEdgeEffectStyle` — 6 new members

**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: ScrollEdgeEffectStyle, b: ScrollEdgeEffectStyle) -> Bool
```
**`automatic`** — The automatic scroll pocket style.
```swift
static var automatic: ScrollEdgeEffectStyle { get }
```
**`hard`** — A scroll edge effect with a hard cutoff and dividing line.
```swift
static var hard: ScrollEdgeEffectStyle { get }
```
**`hash(into:)`** — Hashes the essential components of this value by feeding them into the given hasher.  Implement this method to conform t
```swift
func hash(into hasher: inout Hasher)
```
**`hashValue`** — The hash value.  Hash values are not guaranteed to be equal across different executions of your program. Do not save has
```swift
var hashValue: Int { get }
```
**`soft`** — A scroll edge effect with a soft edge.
```swift
static var soft: ScrollEdgeEffectStyle { get }
```

### `ScrollTargetBehavior` — 2 new members

**`viewAligned(anchor:)`** — The scroll behavior that aligns scroll targets to view-based geometry.  You use this behavior when a scroll view should 
```swift
static func viewAligned(anchor: UnitPoint?) -> Self
```
**`viewAligned(limitBehavior:anchor:)`** — The scroll behavior that aligns scroll targets to view-based geometry.  You use this behavior when a scroll view should 
```swift
static func viewAligned(limitBehavior: ViewAlignedScrollTargetBehavior.LimitBehavior, anchor: UnitPoint?) -> Self
```

### `ScrollView` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `ScrollViewReader` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `SearchToolbarBehavior` — 4 new members

**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: SearchToolbarBehavior, b: SearchToolbarBehavior) -> Bool
```
**`automatic`** — The automatic behavior.
```swift
static var automatic: SearchToolbarBehavior { get }
```
**`hash(into:)`** — Hashes the essential components of this value by feeding them into the given hasher.  Implement this method to conform t
```swift
func hash(into hasher: inout Hasher)
```
**`hashValue`** — The hash value.  Hash values are not guaranteed to be equal across different executions of your program. Do not save has
```swift
var hashValue: Int { get }
```

### `SearchUnavailableContent` — 222 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `Section` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `SecureField` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `SensoryFeedback` — 45 new members

**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: SensoryFeedback.Weight, b: SensoryFeedback.Weight) -> Bool
```
**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: SensoryFeedback.Flexibility, b: SensoryFeedback.Flexibility) -> Bool
```
**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: SensoryFeedback, b: SensoryFeedback) -> Bool
```
**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: SensoryFeedback.SelectionFeedback, b: SensoryFeedback.SelectionFeedback) -> Bool
```
**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: SensoryFeedback.PressFeedback, b: SensoryFeedback.PressFeedback) -> Bool
```
**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: SensoryFeedback.ReleaseFeedback, b: SensoryFeedback.ReleaseFeedback) -> Bool
```
**`alignment`** — Indicates the alignment of a dragged item.  For example, use this pattern in a drawing app when the user drags a shape i
```swift
static let alignment: SensoryFeedback
```
**`button`** — Indicates that a button has been pressed (touch down).  Only plays feedback on visionOS.
```swift
static let button: SensoryFeedback.PressFeedback
```
**`buttonIconOnly`** — Indicates that an icon-only button, without text, has been pressed (touch down).  Only plays feedback on visionOS.
```swift
static let buttonIconOnly: SensoryFeedback.PressFeedback
```
**`decrease`** — Indicates that an important value decreased below a significant threshold.  Only plays feedback on watchOS and visionOS.
```swift
static let decrease: SensoryFeedback
```
**`error`** — Indicates that an error has occurred.  Only plays feedback on iOS and watchOS.
```swift
static let error: SensoryFeedback
```
**`hash(into:)`** — Hashes the essential components of this value by feeding them into the given hasher.  Implement this method to conform t
```swift
func hash(into hasher: inout Hasher)
```
**`hash(into:)`** — Hashes the essential components of this value by feeding them into the given hasher.  Implement this method to conform t
```swift
func hash(into hasher: inout Hasher)
```
**`hash(into:)`** — Hashes the essential components of this value by feeding them into the given hasher.  Implement this method to conform t
```swift
func hash(into hasher: inout Hasher)
```
**`hashValue`** — The hash value.  Hash values are not guaranteed to be equal across different executions of your program. Do not save has
```swift
var hashValue: Int { get }
```
**`hashValue`** — The hash value.  Hash values are not guaranteed to be equal across different executions of your program. Do not save has
```swift
var hashValue: Int { get }
```
**`hashValue`** — The hash value.  Hash values are not guaranteed to be equal across different executions of your program. Do not save has
```swift
var hashValue: Int { get }
```
**`heavy`** — Indicates a collision between large or heavyweight UI objects.
```swift
static let heavy: SensoryFeedback.Weight
```
**`impact`** — Provides a physical metaphor you can use to complement a visual experience.  Use this to provide feedback for UI element
```swift
static let impact: SensoryFeedback
```
**`impact(flexibility:intensity:)`** — Provides a physical metaphor you can use to complement a visual experience.  Use this to provide feedback for UI element
```swift
static func impact(flexibility: SensoryFeedback.Flexibility, intensity: Double = 1.0) -> SensoryFeedback
```
**`impact(weight:intensity:)`** — Provides a physical metaphor you can use to complement a visual experience.  Use this to provide feedback for UI element
```swift
static func impact(weight: SensoryFeedback.Weight = .medium, intensity: Double = 1.0) -> SensoryFeedback
```
**`increase`** — Indicates that an important value increased above a significant threshold.  Only plays feedback on watchOS and visionOS.
```swift
static let increase: SensoryFeedback
```
**`levelChange`** — Indicates movement between discrete levels of pressure.  For example, as the user presses a fast-forward button on a vid
```swift
static let levelChange: SensoryFeedback
```
**`light`** — Indicates a collision between small or lightweight UI objects.
```swift
static let light: SensoryFeedback.Weight
```
**`maximum`** — Indicates that a control's maximum value has been reached, such as when a slider's thumb is dragged to the slider's maxi
```swift
static let maximum: SensoryFeedback.SelectionFeedback
```
**`medium`** — Indicates a collision between medium-sized or medium-weight UI objects.
```swift
static let medium: SensoryFeedback.Weight
```
**`minimum`** — Indicates that a control's minimum value has been reached, such as when a slider's thumb is dragged to the slider's mini
```swift
static let minimum: SensoryFeedback.SelectionFeedback
```
**`off`** — Indicates that a control's value has changed to off, such as when a toggle is released and turned off.  Only plays feedb
```swift
static let off: SensoryFeedback.SelectionFeedback
```
**`on`** — Indicates that a control's value has changed to on, such as when a toggle is tapped to turn on.  Only plays feedback on 
```swift
static let on: SensoryFeedback.SelectionFeedback
```
**`pathComplete`** — Indicates a drawn path has completed and/or recognized.  Use this to provide feedback for closed shape drawing or simila
```swift
static let pathComplete: SensoryFeedback
```
**`press(_:)`** — Plays feedback in response to a specific UI element being pressed (touch down).
```swift
static func press(_ feedback: SensoryFeedback.PressFeedback) -> SensoryFeedback
```
**`release(_:)`** — Plays feedback in response to a specific UI element being released (touch up).
```swift
static func release(_ feedback: SensoryFeedback.ReleaseFeedback) -> SensoryFeedback
```
**`rigid`** — Indicates a collision between hard or inflexible UI objects.
```swift
static let rigid: SensoryFeedback.Flexibility
```
**`selection`** — Indicates that a UI element’s values are changing.  Equivalent to ``selection(_:)`` with ``SelectionFeedback/default``. 
```swift
static let selection: SensoryFeedback
```
**`selection(_:)`** — Plays feedback in response to a specific UI element's values changing.
```swift
static func selection(_ feedback: SensoryFeedback.SelectionFeedback) -> SensoryFeedback
```
**`slider`** — Indicates that a slider's thumb has been pressed (touch down).  Slider controls should also play ``SensoryFeedback/relea
```swift
static let slider: SensoryFeedback.PressFeedback
```
**`slider`** — Indicates that a slider's thumb has been released (touch up).  Slider controls should also play ``SensoryFeedback/press(
```swift
static let slider: SensoryFeedback.ReleaseFeedback
```
**`soft`** — Indicates a collision between soft or flexible UI objects.
```swift
static let soft: SensoryFeedback.Flexibility
```
**`solid`** — Indicates a collision between solid UI objects of medium flexibility.
```swift
static let solid: SensoryFeedback.Flexibility
```
**`start`** — Indicates that an activity started.  Use this haptic when starting a timer or any other activity that can be explicitly 
```swift
static let start: SensoryFeedback
```
**`stop`** — Indicates that an activity stopped.  Use this haptic when stopping a timer or other activity that was previously started
```swift
static let stop: SensoryFeedback
```
**`success`** — Indicates that a task or action has completed.  Only plays feedback on iOS and watchOS.
```swift
static let success: SensoryFeedback
```
**`tab`** — Indicates that a control to activate a tab has been pressed (touch down).  Only plays feedback on visionOS.
```swift
static let tab: SensoryFeedback.PressFeedback
```
**`toggle`** — Indicates that a toggle has been pressed (touch down).  Toggle controls should also play ``SensoryFeedback/selection(_:)
```swift
static let toggle: SensoryFeedback.PressFeedback
```
**`warning`** — Indicates that a task or action has produced a warning of some kind.  Only plays feedback on iOS and watchOS.
```swift
static let warning: SensoryFeedback
```

### `Settings` — 2 new members

**`defaultLaunchBehavior(_:)`** — Sets the default launch behavior for this scene.  This behavior can be used to define if a scene is shown on application
```swift
nonisolated func defaultLaunchBehavior(_ behavior: SceneLaunchBehavior) -> some Scene
```
**`immersionStyle(selection:in:)`** — Sets the style for an immersive space.  Use this modifier to configure the appearance and behavior of an ``ImmersiveSpac
```swift
nonisolated func immersionStyle(selection: Binding<any ImmersionStyle>, in styles: any ImmersionStyle...) -> some Scene
```

### `SettingsLink` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `ShareLink` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `Slider` — 78 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`init(value:in:neutralValue:enabledBounds:label:currentValueLabel:minimumValueLabel:maximumValueLabel:onEditingChanged:)`** — Creates a slider to select a value from a given range, which displays the provided labels.  - Parameters:   - value: The
**`init(value:in:neutralValue:enabledBounds:label:currentValueLabel:minimumValueLabel:maximumValueLabel:ticks:onEditingChanged:)`** — Creates a slider to select a value from a given range, which displays the provided labels and customized ticks.  - Param
**`init(value:in:step:neutralValue:enabledBounds:label:currentValueLabel:minimumValueLabel:maximumValueLabel:tick:onEditingChanged:)`** — Creates a slider to select a value from a given range, subject to a step increment, which displays the provided labels a
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `SliderTick` — 14 new members

**`<(_:_:)`** — Returns a Boolean value indicating whether the value of the first argument is less than that of the second argument.  Th
```swift
static func < (lhs: SliderTick<V>, rhs: SliderTick<V>) -> Bool
```
**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: SliderTick<V>.ID, b: SliderTick<V>.ID) -> Bool
```
**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (lhs: SliderTick<V>, rhs: SliderTick<V>) -> Bool
```
**`SliderTick.Body`**
```swift
typealias Body = Array<SliderTick<V>>
```
**`SliderTick.Value`**
```swift
typealias Value = V
```
**`body`** — The value of this type's content.
```swift
var body: SliderTick<V>.Body { get }
```
**`hash(into:)`** — Hashes the essential components of this value by feeding them into the given hasher.  Implement this method to conform t
```swift
func hash(into hasher: inout Hasher)
```
**`hashValue`** — The hash value.  Hash values are not guaranteed to be equal across different executions of your program. Do not save has
```swift
var hashValue: Int { get }
```
**`id`** — The identity of a tick, which is derived from its value.
```swift
var id: SliderTick<V>.ID { get }
```
**`init(_:)`** — Create a labeled slider tick at a specific value.
```swift
init(_ value: V)
```
**`init(_:_:)`** — Create a slider tick with a label from a localized string key.
```swift
init(_ titleKey: LocalizedStringKey, _ value: V)
```
**`init(_:_:)`** — Create a slider tick with a label from a localized string resource.
```swift
init(_ titleResource: LocalizedStringResource, _ value: V)
```
**`init(_:_:)`** — Create a slider tick with a label from a string title.
```swift
init<S>(_ title: S, _ value: V) where S : StringProtocol
```
**`init(_:label:)`** — Create a labeled slider tick at a specific value.
```swift
init(_ value: V, @ViewBuilder label: () -> some View)
```

### `SliderTickBuilder` — 15 new members

**`buildBlock()`** — Creates a single slider content result.
```swift
static func buildBlock() -> some SliderTickContent<V>
```
**`buildBlock(_:)`** — Creates a single slider content result.
```swift
static func buildBlock(_ content: some SliderTickContent<V>) -> some SliderTickContent<V>
```
**`buildBlock(_:_:)`**
```swift
static func buildBlock<C0, C1>(_ c0: C0, _ c1: C1) -> some SliderTickContent<V> where V == C0.Value, C0 : SliderTickContent, C1 : SliderTickContent, C0.Value == C1.Value
```
**`buildBlock(_:_:_:)`**
**`buildBlock(_:_:_:_:)`**
**`buildBlock(_:_:_:_:_:)`**
**`buildBlock(_:_:_:_:_:_:)`**
**`buildBlock(_:_:_:_:_:_:_:)`**
**`buildBlock(_:_:_:_:_:_:_:_:)`**
**`buildBlock(_:_:_:_:_:_:_:_:_:)`**
**`buildBlock(_:_:_:_:_:_:_:_:_:_:)`**
**`buildEither(first:)`** — Produces content for a conditional statement in a multi-statement closure when the condition is true.
```swift
static func buildEither<T, F>(first: T) -> _ConditionalContent<T, F> where V == T.Value, T : SliderTickContent, F : SliderTickContent, T.Body == F.Body
```
**`buildEither(second:)`** — Produces content for a conditional statement in a multi-statement closure when the condition is false.
```swift
static func buildEither<T, F>(second: F) -> _ConditionalContent<T, F> where V == T.Value, T : SliderTickContent, F : SliderTickContent, T.Body == F.Body
```
**`buildExpression(_:)`** — Creates a single slider content expression.
```swift
static func buildExpression(_ content: some SliderTickContent<V>) -> some SliderTickContent<V>
```
**`buildIf(_:)`** — Produces an optional slider content for conditional statements in multi-statement closures that's only visible when the 
```swift
static func buildIf(_ content: some SliderTickContent<V>) -> some SliderTickContent<V>
```

### `SliderTickContent` — 3 new members

**`Body`**
```swift
associatedtype Body : RandomAccessCollection where Self.Body.Element == SliderTick<Self.Value>
```
**`Value`**
```swift
associatedtype Value : BinaryFloatingPoint
```
**`body`** — The value of this type's content.
```swift
var body: Self.Body { get }
```

### `SliderTickContentForEach` — 6 new members

**`SliderTickContentForEach.Body`**
```swift
typealias Body = [SliderTick<SliderTickContentForEach<Data, ID, Content>.Value>]
```
**`SliderTickContentForEach.Value`**
```swift
typealias Value = Content.Value
```
**`body`** — The value of this type's content.
```swift
var body: [SliderTick<SliderTickContentForEach<Data, ID, Content>.Value>] { get }
```
**`init(_:content:)`** — Creates an instance that uniquely identifies and creates slider ticks across updates based on the identity of the underl
```swift
nonisolated init<V>(_ data: Data, @SliderTickBuilder<V> content: @escaping (Data.Element) -> Content) where ID == V.ID, V : Identifiable, V == Data.Element, Data.Element == Content.Value
```
**`init(_:content:)`** — Creates an instance that computes slider ticks on demand over a given constant range.  The instance only reads the initi
```swift
nonisolated init<V>(_ data: Range<Int>, @SliderTickBuilder<V> content: @escaping (Range<Int>.Element) -> Content) where Data == Range<Int>, V == Content.Value
```
**`init(_:id:content:)`** — Creates an instance that uniquely identifies and creates slider ticks across updates based on the provided key path to t
```swift
nonisolated init<V>(_ data: Data, id: KeyPath<Data.Element, ID>, @SliderTickBuilder<V> content: @escaping (Data.Element) -> Content) where V == Data.Element, Data.Element == Content.Value
```

### `SpacerSizing` — 2 new members

**`fixed`** — The fixed spacer sizing behavior. The spacer will use a pre-defined size determined by the system and the context in whi
```swift
static let fixed: SpacerSizing
```
**`flexible`** — The flexible spacer sizing behavior. The spacer will expand to accommodate as much space as it is given in the current c
```swift
static let flexible: SpacerSizing
```

### `SpatialEventCollection` — 5 new members

**`SpatialEventCollection.Event.Kind.indirectPinch`** — An event generated from an indirectly targeted pinching hand.
```swift
case indirectPinch
```
**`chirality`** — The hand chirality (left or right) of this event, for relevant event kinds.
```swift
var chirality: Chirality?
```
**`inputDevicePose`** — The 3D position and orientation of the device controlling the touch, if one exists.
```swift
var inputDevicePose: SpatialEventCollection.Event.InputDevicePose? { get set }
```
**`pose3D`** — The 3D pose of the input device.
```swift
var pose3D: Pose3D
```
**`selectionRay`** — The 3D ray used to target the touch.
```swift
var selectionRay: Ray3D?
```

### `Stepper` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `SubscriptionView` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `SurroundingsEffect` — 6 new members

**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: SurroundingsEffect, b: SurroundingsEffect) -> Bool
```
**`colorMultiply(_:)`** — An effect that applies a custom tint to the passthrough video by multiplying the passthrough with a ``Color``.  Use this
```swift
static func colorMultiply(_ color: Color) -> SurroundingsEffect
```
**`dark`** — An effect that dims passthrough video.  Use this value with the ``View/preferredSurroundingsEffect(_:)`` view modifier w
```swift
static var dark: SurroundingsEffect { get }
```
**`dim(intensity:)`** — An effect that dims the passthrough video a custom amount.  Use this with the ``View/preferredSurroundingsEffect(_:)`` v
```swift
static func dim(intensity: Double) -> SurroundingsEffect
```
**`semiDark`** — An effect that dims passthrough video less than ``SurroundingsEffect/dark``.  Use this value with the ``View/preferredSu
```swift
static var semiDark: SurroundingsEffect { get }
```
**`ultraDark`** — An effect that dims passthrough video more than ``SurroundingsEffect/dark``  Use this value with the ``View/preferredSur
```swift
static var ultraDark: SurroundingsEffect { get }
```

### `Tab` — 12 new members

**`init(_:image:content:)`** — Creates a new tab that you can use in a tab view, with a localized string resource.  - Parameters:     - titleResource: 
```swift
init(_ titleResource: LocalizedStringResource, image: String, @ViewBuilder content: () -> Content) where Label == DefaultTabLabel
```
**`init(_:image:role:content:)`** — Creates a new tab that you can use in a tab view, with a localized string resource.  - Parameters:     - titleResource: 
```swift
init(_ titleResource: LocalizedStringResource, image: String, role: TabRole?, @ViewBuilder content: () -> Content) where Label == DefaultTabLabel
```
**`init(_:image:value:content:)`** — Creates a tab that the tab view presents when the tab view's selection matches the tab's value, with a localized string 
```swift
nonisolated init(_ titleResource: LocalizedStringResource, image: String, value: Value, @ViewBuilder content: () -> Content) where Label == DefaultTabLabel
```
**`init(_:image:value:content:)`** — Creates a tab that the tab view presents when the tab view's selection matches the tab's value, with a localized string 
```swift
nonisolated init<T>(_ titleResource: LocalizedStringResource, image: String, value: T, @ViewBuilder content: () -> Content) where Value == T?, Label == DefaultTabLabel, T : Hashable
```
**`init(_:image:value:role:content:)`** — Creates a tab that the tab view presents when the tab view's selection matches the tab's value, with a localized string 
```swift
nonisolated init(_ titleResource: LocalizedStringResource, image: String, value: Value, role: TabRole?, @ViewBuilder content: () -> Content) where Label == DefaultTabLabel
```
**`init(_:image:value:role:content:)`** — Creates a tab that the tab view presents when the tab view's selection matches the tab's value, with a localized string 
```swift
nonisolated init<T>(_ titleResource: LocalizedStringResource, image: String, value: T, role: TabRole?, @ViewBuilder content: () -> Content) where Value == T?, Label == DefaultTabLabel, T : Hashable
```
**`init(_:systemImage:content:)`** — Creates a new tab that you can use in a tab view using a system image for the tab item's image, and a localized string k
```swift
init(_ titleResource: LocalizedStringResource, systemImage: String, @ViewBuilder content: () -> Content) where Label == DefaultTabLabel
```
**`init(_:systemImage:role:content:)`** — Creates a new tab that you can use in a tab view using a system image for the tab item's image, and a localized string k
```swift
init(_ titleResource: LocalizedStringResource, systemImage: String, role: TabRole?, @ViewBuilder content: () -> Content) where Label == DefaultTabLabel
```
**`init(_:systemImage:value:content:)`** — Creates a tab that the tab view presents when the tab view's selection matches the tab's value using a system image for 
```swift
nonisolated init(_ titleResource: LocalizedStringResource, systemImage: String, value: Value, @ViewBuilder content: () -> Content) where Label == DefaultTabLabel
```
**`init(_:systemImage:value:content:)`** — Creates a tab that the tab view presents when the tab view's selection matches the tab's value using a system image for 
```swift
nonisolated init<T>(_ titleResource: LocalizedStringResource, systemImage: String, value: T, @ViewBuilder content: () -> Content) where Value == T?, Label == DefaultTabLabel, T : Hashable
```
**`init(_:systemImage:value:role:content:)`** — Creates a tab that the tab view presents when the tab view's selection matches the tab's value using a system image for 
```swift
nonisolated init(_ titleResource: LocalizedStringResource, systemImage: String, value: Value, role: TabRole?, @ViewBuilder content: () -> Content) where Label == DefaultTabLabel
```
**`init(_:systemImage:value:role:content:)`** — Creates a tab that the tab view presents when the tab view's selection matches the tab's value using a system image for 

### `TabBarMinimizeBehavior` — 4 new members

**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: TabBarMinimizeBehavior, b: TabBarMinimizeBehavior) -> Bool
```
**`automatic`** — Determine the behavior automatically based on the surrounding context.  The depends on the platform: * On iOS, iPadOS, t
```swift
static let automatic: TabBarMinimizeBehavior
```
**`hash(into:)`** — Hashes the essential components of this value by feeding them into the given hasher.  Implement this method to conform t
```swift
func hash(into hasher: inout Hasher)
```
**`hashValue`** — The hash value.  Hash values are not guaranteed to be equal across different executions of your program. Do not save has
```swift
var hashValue: Int { get }
```

### `TabContentBuilder` — 74 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `TabSearchActivation` — 5 new members

**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: TabSearchActivation, b: TabSearchActivation) -> Bool
```
**`automatic`** — The default search activation behavior.  By default, search will only activate when the user interacts with the search f
```swift
static var automatic: TabSearchActivation { get }
```
**`hash(into:)`** — Hashes the essential components of this value by feeding them into the given hasher.  Implement this method to conform t
```swift
func hash(into hasher: inout Hasher)
```
**`hashValue`** — The hash value.  Hash values are not guaranteed to be equal across different executions of your program. Do not save has
```swift
var hashValue: Int { get }
```
**`searchTabSelection`** — Links the search tab's selection to search activation.  When the search tab is selected, search will activate. When the 
```swift
static var searchTabSelection: TabSearchActivation { get }
```

### `TabView` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `TabViewBottomAccessoryPlacement` — 5 new members

**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: TabViewBottomAccessoryPlacement, b: TabViewBottomAccessoryPlacement) -> Bool
```
**`TabViewBottomAccessoryPlacement.expanded`** — The bar is expanded on top of the bottom tab bar, if there is a bottom tab bar, or at the bottom of the tab's content vi
```swift
case expanded
```
**`TabViewBottomAccessoryPlacement.inline`** — The view is displayed in line with the bottom tab bar.
```swift
case inline
```
**`hash(into:)`** — Hashes the essential components of this value by feeding them into the given hasher.  Implement this method to conform t
```swift
func hash(into hasher: inout Hasher)
```
**`hashValue`** — The hash value.  Hash values are not guaranteed to be equal across different executions of your program. Do not save has
```swift
var hashValue: Int { get }
```

### `Table` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `TextEditor` — 76 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`init(text:selection:)`** — Creates a styled text editor.  Use a ``TextEditor`` instance to create a view in which users can enter and edit long-for
```swift
init(text: Binding<AttributedString>, selection: Binding<AttributedTextSelection>? = nil)
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `TextField` — 76 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`init(_:text:)`** — Creates a text field with a text label generated from a localized title string resource.  - Parameters:   - titleResourc
```swift
nonisolated init(_ titleResource: LocalizedStringResource, text: Binding<String>)
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `TextInputFormattingControlPlacement` — 10 new members

**`TextInputFormattingControlPlacement.Set.ArrayLiteralElement`** — The type of the elements of an array literal.
```swift
typealias ArrayLiteralElement = TextInputFormattingControlPlacement.Set
```
**`TextInputFormattingControlPlacement.Set.Element`** — The element type of the option set.  To inherit all the default implementations from the `OptionSet` protocol, the `Elem
```swift
typealias Element = TextInputFormattingControlPlacement.Set
```
**`TextInputFormattingControlPlacement.Set.RawValue`** — The raw type that can be used to represent all values of the conforming type.  Every distinct value of the conforming ty
```swift
typealias RawValue = UInt
```
**`accessoryBar`** — Text formatting controls placed beneath the window toolbar.
```swift
static let accessoryBar: TextInputFormattingControlPlacement.Set
```
**`all`**
```swift
static let all: TextInputFormattingControlPlacement.Set
```
**`contextMenu`** — Text formatting controls placed in the context menu, and the edit menu on iOS.  On macOS, the Font Panel is available on
```swift
static let contextMenu: TextInputFormattingControlPlacement.Set
```
**`default`**
```swift
static let `default`: TextInputFormattingControlPlacement.Set
```
**`fontPanel`** — Text formatting controls for choosing fonts that appear in a dedicated window on macOS.
```swift
static let fontPanel: TextInputFormattingControlPlacement.Set
```
**`init(rawValue:)`** — Creates a new option set from the given raw value.  This initializer always succeeds, even if the value passed as `rawVa
```swift
init(rawValue: UInt)
```
**`rawValue`** — The corresponding value of the raw type.  A new instance initialized with `rawValue` will be equivalent to this instance
```swift
let rawValue: UInt
```

### `TimelineView` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `Toggle` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `ToggleStyleConfiguration` — 74 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `ToolbarContent` — 1 new members

**`sharedBackgroundVisibility(_:)`** — Controls the visibility of the glass background effect on items in the toolbar. In certain contexts, such as the navigat
```swift
nonisolated func sharedBackgroundVisibility(_ visibility: Visibility) -> some ToolbarContent
```

### `ToolbarDefaultItemKind` — 1 new members

**`search`** — The search item added by a ``View/searchable(text:isPresented:placement:prompt)`` modifier.  Use a `.search` default ite
```swift
static let search: ToolbarDefaultItemKind
```

### `ToolbarItem` — 2 new members

**`sharedBackgroundVisibility(_:)`** — Controls the visibility of the glass background effect on items in the toolbar. In certain contexts, such as the navigat
```swift
nonisolated func sharedBackgroundVisibility(_ visibility: Visibility) -> some ToolbarContent
```
**`sharedBackgroundVisibility(_:)`** — Controls the visibility of the glass background effect on items in the toolbar. In certain contexts, such as the navigat
```swift
nonisolated func sharedBackgroundVisibility(_ visibility: Visibility) -> some CustomizableToolbarContent
```

### `ToolbarItemGroup` — 1 new members

**`sharedBackgroundVisibility(_:)`** — Controls the visibility of the glass background effect on items in the toolbar. In certain contexts, such as the navigat
```swift
nonisolated func sharedBackgroundVisibility(_ visibility: Visibility) -> some ToolbarContent
```

### `ToolbarSpacer` — 3 new members

**`ToolbarSpacer.Body`** — The type of content representing the body of this toolbar content.
```swift
typealias Body = Never
```
**`init(_:placement:)`** — Creates a toolbar spacer item with the specified sizing behavior and placement.
```swift
init(_ sizing: SpacerSizing = .flexible, placement: ToolbarItemPlacement = .automatic)
```
**`sharedBackgroundVisibility(_:)`** — Controls the visibility of the glass background effect on items in the toolbar. In certain contexts, such as the navigat
```swift
nonisolated func sharedBackgroundVisibility(_ visibility: Visibility) -> some CustomizableToolbarContent
```

### `ToolbarTitleMenu` — 2 new members

**`sharedBackgroundVisibility(_:)`** — Controls the visibility of the glass background effect on items in the toolbar. In certain contexts, such as the navigat
```swift
nonisolated func sharedBackgroundVisibility(_ visibility: Visibility) -> some CustomizableToolbarContent
```
**`sharedBackgroundVisibility(_:)`** — Controls the visibility of the glass background effect on items in the toolbar. In certain contexts, such as the navigat
```swift
nonisolated func sharedBackgroundVisibility(_ visibility: Visibility) -> some ToolbarContent
```

### `TupleSliderTickContent` — 5 new members

**`TupleSliderTickContent.Body`**
```swift
typealias Body = [SliderTick<V>]
```
**`TupleSliderTickContent.TicksCollection`**
```swift
typealias TicksCollection = [SliderTick<V>]
```
**`TupleSliderTickContent.Value`**
```swift
typealias Value = V
```
**`body`** — The value of this type's content.
```swift
var body: [SliderTick<V>] { get }
```
**`value`**
```swift
var value: T
```

### `UtilityWindow` — 2 new members

**`defaultLaunchBehavior(_:)`** — Sets the default launch behavior for this scene.  This behavior can be used to define if a scene is shown on application
```swift
nonisolated func defaultLaunchBehavior(_ behavior: SceneLaunchBehavior) -> some Scene
```
**`immersionStyle(selection:in:)`** — Sets the style for an immersive space.  Use this modifier to configure the appearance and behavior of an ``ImmersiveSpac
```swift
nonisolated func immersionStyle(selection: Binding<any ImmersionStyle>, in styles: any ImmersionStyle...) -> some Scene
```

### `VSplitView` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `ViewAlignedScrollTargetBehavior` — 4 new members

**`init(anchor:)`** — Creates a view aligned scroll behavior with the provided anchor.
```swift
init(anchor: UnitPoint?)
```
**`init(limitBehavior:anchor:)`** — Creates a view aligned scroll behavior with the provided limit behavior and anchor.
```swift
init(limitBehavior: ViewAlignedScrollTargetBehavior.LimitBehavior, anchor: UnitPoint?)
```
**`viewAligned(anchor:)`** — The scroll behavior that aligns scroll targets to view-based geometry.  You use this behavior when a scroll view should 
```swift
static func viewAligned(anchor: UnitPoint?) -> Self
```
**`viewAligned(limitBehavior:anchor:)`** — The scroll behavior that aligns scroll targets to view-based geometry.  You use this behavior when a scroll view should 
```swift
static func viewAligned(limitBehavior: ViewAlignedScrollTargetBehavior.LimitBehavior, anchor: UnitPoint?) -> Self
```

### `ViewThatFits` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `WidgetBundleBuilder` — 2 new members

**`buildExpression(_:)`** — Builds an expression within the builder.
```swift
static func buildExpression<Content>(_ content: Content) -> some Widget where Content : ControlWidget
```
**`buildLimitedAvailability(_:)`** — Builds an availability check within the builder
```swift
static func buildLimitedAvailability(_ widget: some ControlWidget) -> any Widget & _LimitedAvailabilityWidgetMarker
```

### `Window` — 8 new members

**`Window.Body`** — The type of scene that represents the body of this scene.  When you create a custom scene, Swift infers this type from y
```swift
typealias Body = some Scene
```
**`body`** — The content and behavior of the scene.  For any scene that you create, provide a computed `body` property that defines t
```swift
@MainActor @preconcurrency var body: some Scene { get }
```
**`defaultLaunchBehavior(_:)`** — Sets the default launch behavior for this scene.  This behavior can be used to define if a scene is shown on application
```swift
nonisolated func defaultLaunchBehavior(_ behavior: SceneLaunchBehavior) -> some Scene
```
**`immersionStyle(selection:in:)`** — Sets the style for an immersive space.  Use this modifier to configure the appearance and behavior of an ``ImmersiveSpac
```swift
nonisolated func immersionStyle(selection: Binding<any ImmersionStyle>, in styles: any ImmersionStyle...) -> some Scene
```
**`init(_:id:content:)`** — Creates a window with a title and an identifier.  The window displays the view that you specify.  > Important: The syste
```swift
init(_ title: Text, id: String, @ViewBuilder content: () -> Content)
```
**`init(_:id:content:)`** — Creates a window with a localized title and an identifier.  The window displays the view that you specify.  - Parameters
```swift
init(_ titleKey: LocalizedStringKey, id: String, @ViewBuilder content: () -> Content)
```
**`init(_:id:content:)`** — Creates a window with a localized title and an identifier.  The window displays the view that you specify.  - Parameters
```swift
init(_ titleResource: LocalizedStringResource, id: String, @ViewBuilder content: () -> Content)
```
**`init(_:id:content:)`** — Creates a window with a title string and an identifier.  The window displays the view that you specify.  - Parameters:  
```swift
init<S>(_ title: S, id: String, @ViewBuilder content: () -> Content) where S : StringProtocol
```

### `WindowGroup` — 2 new members

**`defaultLaunchBehavior(_:)`** — Sets the default launch behavior for this scene.  This behavior can be used to define if a scene is shown on application
```swift
nonisolated func defaultLaunchBehavior(_ behavior: SceneLaunchBehavior) -> some Scene
```
**`immersionStyle(selection:in:)`** — Sets the style for an immersive space.  Use this modifier to configure the appearance and behavior of an ``ImmersiveSpac
```swift
nonisolated func immersionStyle(selection: Binding<any ImmersionStyle>, in styles: any ImmersionStyle...) -> some Scene
```

### `WindowVisibilityToggle` — 75 new members

**`accessibilityDefaultFocus(_:_:)`** — Defines a region in which default accessibility focus is evaluated by assigning a value to a given accessibility focus s
```swift
nonisolated func accessibilityDefaultFocus<Value>(_ binding: AccessibilityFocusState<Value>.Binding, _ value: Value) -> some View where Value : Hashable
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: Text, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ statusKey: LocalizedStringKey, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: some StringProtocol, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`accessibilityScrollStatus(_:isEnabled:)`** — Changes the announcement provided by accessibility technologies when a user scrolls a scroll view within this view.  Use
```swift
nonisolated func accessibilityScrollStatus(_ status: LocalizedStringResource, isEnabled: Bool = true) -> ModifiedContent<Self, AccessibilityAttachmentModifier>
```
**`assistiveAccessNavigationIcon(_:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(_ icon: Image) -> some View
```
**`assistiveAccessNavigationIcon(systemImage:)`** — Configures the view's icon for purposes of navigation.  In an Assistive Access scene on iOS and iPadOS, the icon is disp
```swift
nonisolated func assistiveAccessNavigationIcon(systemImage: String) -> some View
```
**`attributedTextFormattingDefinition(_:)`** — Apply a text formatting definition to nested views.  Applying a text formatting definition to a ``Text`` or ``TextEditor
```swift
nonisolated func attributedTextFormattingDefinition<D>(_ definition: D) -> some View where D : AttributedTextFormattingDefinition
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ scope: S.Type) -> some View where S : AttributeScope
```
**`attributedTextFormattingDefinition(_:)`** — Apply an attribute-only text formatting definition to all nested editor views.  Using only an attribute scope for the te
```swift
nonisolated func attributedTextFormattingDefinition<S>(_ path: KeyPath<AttributeScopes, S.Type>) -> some View where S : AttributeScope
```
**`backgroundExtensionEffect()`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect() -> some View
```
**`backgroundExtensionEffect(isEnabled:)`** — Adds the background extension effect to the view. The view will be duplicated into mirrored copies which will be placed 
```swift
@MainActor @preconcurrency func backgroundExtensionEffect(isEnabled: Bool) -> some View
```
**`buttonSizing(_:)`** — The preferred sizing behavior of buttons in the view hierarchy.  Views may use the specified button sizing when determin
```swift
nonisolated func buttonSizing(_ sizing: ButtonSizing) -> some View
```
**`containerCornerOffset(_:sizeToFit:)`** — Adjusts the view's layout to avoid the container view's corner insets for the specified edges.  Use this modifier when y
```swift
nonisolated func containerCornerOffset(_ edges: Edge.Set, sizeToFit: Bool = false) -> some View
```
**`containerShape(_:)`** — Sets the container shape to use for any container relative shape or concentric rectangle within this view.  The example 
```swift
nonisolated func containerShape(_ shape: some RoundedRectangularShape) -> some View
```
**`controlSize(_:)`** — Limits the control size within the view to the given range.  Use this modifier if you're implementing a custom control o
```swift
nonisolated func controlSize<T>(_ range: T) -> some View where T : RangeExpression, T.Bound == ControlSize
```
**`dragConfiguration(_:)`** — Configures a drag session.  Below is a simplified example of a view that supports copy, move and delete operations for d
```swift
nonisolated func dragConfiguration(_ configuration: DragConfiguration) -> some View
```
**`dragContainer(for:in:_:)`** — A container with draggable views where the drag payload is based on multiple identifiers of dragged items.  Provide the 
**`dragContainer(for:in:_:)`** — A container with draggable views. The drag payload is identifiable. To form the payload, use the identifier of the dragg
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views. The drag payload is based on the current selection.  Below is an example of a containe
**`dragContainer(for:itemID:in:_:)`** — A container with draggable views.  In an example below, an app presents a view with `Fruit` values. `Fruit` does not con
**`dragContainerSelection(_:containerNamespace:)`** — Provides multiple item selection support for drag containers.  A drag container finds the nearest enclosing `dragContain
**`dragPreviewsFormation(_:)`** — Describes the way dragged previews are visually composed.
```swift
nonisolated func dragPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`draggable(_:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(_:id:containerNamespace:_:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:id:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional payload and specify the nam
**`draggable(_:item:containerNamespace:)`** — Activates this view as the source of a drag and drop operation, allowing to provide optional identifiable payload and sp
**`draggable(containerItemID:containerNamespace:)`** — Inside a drag container, activates this view as the source of a drag and drop operation. Supports lazy drag containers. 
```swift
nonisolated func draggable<ItemID>(containerItemID: ItemID, containerNamespace: Namespace.ID? = nil) -> some View where ItemID : Hashable, ItemID : Sendable
```
**`dropConfiguration(_:)`** — Configures a drop session.  Below is an example of a view that accepts drop of `Image` type. The view prefers drop opera
```swift
nonisolated func dropConfiguration(_ configuration: @escaping (DropSession) -> DropConfiguration) -> some View
```
**`dropDestination(for:isEnabled:action:)`** — Defines the destination of a drag and drop operation that provides a drop operation proposal and handles the dropped con
```swift
nonisolated func dropDestination<T>(for type: T.Type = T.self, isEnabled: Bool = true, action: @escaping ([T], DropSession) -> Void) -> some View where T : Transferable
```
**`dropPreviewsFormation(_:)`** — Describes the way previews for a drop are composed.
```swift
nonisolated func dropPreviewsFormation(_ formation: DragDropPreviewsFormation) -> some View
```
**`findDisabled(_:)`** — Prevents find and replace operations in a text editor.  Add this modifier to ensure that people can't activate the find 
```swift
nonisolated func findDisabled(_ isDisabled: Bool = true) -> some View
```
**`findNavigator(isPresented:)`** — Programmatically presents the find and replace interface for text editor views.  Add this modifier to a ``TextEditor``, 
```swift
nonisolated func findNavigator(isPresented: Binding<Bool>) -> some View
```
**`gesture(_:)`** — Attaches an ``NSGestureRecognizerRepresentable`` to the view.  - Parameters:     - representable: The ``NSGestureRecogni
```swift
nonisolated func gesture(_ representable: some NSGestureRecognizerRepresentable) -> some View
```
**`glassEffect(_:in:)`** — Applies a glass effect to this view.  When you use a glass effect, the platform:   - Renders a shape anchored behind thi
```swift
nonisolated func glassEffect(_ glass: Glass = .regular, in shape: some Shape = DefaultGlassEffectShape()) -> some View
```
**`glassEffectID(_:in:)`** — Associates an identity value to glass effects defined within this view.  You use this modifier with the ``View/glassEffe
```swift
nonisolated func glassEffectID(_ id: (some Hashable & Sendable)?, in namespace: Namespace.ID) -> some View
```
**`glassEffectTransition(_:)`** — Associates a glass effect transition with any glass effects defined within this view.  You use this modifier with the ``
```swift
@MainActor @preconcurrency func glassEffectTransition(_ transition: GlassEffectTransition) -> some View
```
**`glassEffectUnion(id:namespace:)`** — Associates any glass effects defined within this view to a union with the provided id.  You may want the geometries of m
```swift
@MainActor @preconcurrency func glassEffectUnion(id: (some Hashable & Sendable)?, namespace: Namespace.ID) -> some View
```
**`labelIconToTitleSpacing(_:)`** — Set the spacing between the icon and title in labels.  Use this modifier to set the value that should be used for the ic
```swift
nonisolated func labelIconToTitleSpacing(_ value: CGFloat) -> some View
```
**`labelReservedIconWidth(_:)`** — Set the width reserved for icons in labels.  Use this modifier to set the value that should be used for the reserved ico
```swift
nonisolated func labelReservedIconWidth(_ value: CGFloat) -> some View
```
**`lineHeight(_:)`** — A modifier for the default line height in the view hierarchy.  The default value is `nil`. In that case, SwiftUI automat
```swift
nonisolated func lineHeight(_ lineHeight: AttributedString.LineHeight?) -> some View
```
**`listRowInsets(_:_:)`** — Sets the insets of rows in a list on the specified edges.  Use this modifier to change the default insets of list rows o
```swift
nonisolated func listRowInsets(_ edges: Edge.Set = .all, _ length: CGFloat?) -> some View
```
**`multilineTextAlignment(strategy:)`** — A modifier for the default text alignment strategy in the view hierarchy.  To control the alignment explicitly at a view
```swift
nonisolated func multilineTextAlignment(strategy: Text.AlignmentStrategy) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation.  A view's navigation subtitle is used to provide additional c
```swift
nonisolated func navigationSubtitle(_ subtitle: Text) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string.  A view's navigation subtitle is us
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringKey) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a localized string resource.  A view's navigation subti
```swift
nonisolated func navigationSubtitle(_ subtitleKey: LocalizedStringResource) -> some View
```
**`navigationSubtitle(_:)`** — Configures the view's subtitle for purposes of navigation, using a string.  A view's navigation subtitle is used to prov
```swift
nonisolated func navigationSubtitle<S>(_ subtitle: S) -> some View where S : StringProtocol
```
**`onDragSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing dragging operation activated by `draggable(_:)` or anther dr
```swift
nonisolated func onDragSessionUpdated(_ onUpdate: @escaping (DragSession) -> Void) -> some View
```
**`onDropSessionUpdated(_:)`** — Specifies an action to perform on each update of an ongoing drop operation activated by `dropDestination(_:)` or other d
```swift
nonisolated func onDropSessionUpdated(_ onUpdate: @escaping (DropSession) -> Void) -> some View
```
**`onInteractiveResizeChange(_:)`** — Adds an action to perform when the enclosing window is being interactively resized.  Use this modifier to adjust how you
```swift
nonisolated func onInteractiveResizeChange(_ action: @escaping (Bool) -> Void) -> some View
```
**`onOpenURL(prefersInApp:)`** — Sets an `OpenURLAction` that prefers opening URL with an in-app browser. It's equivalent to calling `.onOpenURL(_:)`    
```swift
@MainActor @preconcurrency func onOpenURL(prefersInApp: Bool) -> some View
```
**`onPencilDoubleTap(perform:)`** — Adds an action to perform after the user double-taps their Apple Pencil.  You should respect people’s setting for the do
```swift
nonisolated func onPencilDoubleTap(perform action: @escaping (PencilDoubleTapGestureValue) -> Void) -> some View
```
**`onPencilSqueeze(perform:)`** — Adds an action to perform when the user squeezes their Apple Pencil.  You should respect people’s setting for the squeez
```swift
nonisolated func onPencilSqueeze(perform action: @escaping (PencilSqueezeGesturePhase) -> Void) -> some View
```
**`replaceDisabled(_:)`** — Prevents replace operations in a text editor.  Add this modifier to ensure that people can't activate the replace featur
```swift
nonisolated func replaceDisabled(_ isDisabled: Bool = true) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar above or below the modified view.  Similar to the ``View/safeAreaInset(edge:
```swift
nonisolated func safeAreaBar(edge: VerticalEdge, alignment: HorizontalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`safeAreaBar(edge:alignment:spacing:content:)`** — Shows the specified content as a custom bar beside the modified view.  Similar to the ``View/safeAreaInset(edge:alignmen
```swift
nonisolated func safeAreaBar(edge: HorizontalEdge, alignment: VerticalAlignment = .center, spacing: CGFloat? = nil, @ViewBuilder content: () -> some View) -> some View
```
**`scrollEdgeEffectHidden(_:for:)`** — Hides any scroll edge effects for scroll views within this hierarchy.  By default, a scroll view renders an automatic ed
```swift
nonisolated func scrollEdgeEffectHidden(_ hidden: Bool = true, for edges: Edge.Set = .all) -> some View
```
**`scrollEdgeEffectStyle(_:for:)`** — Configures the scroll edge effect style for scroll views within this hierarchy.  By default, a scroll view renders an au
```swift
nonisolated func scrollEdgeEffectStyle(_ style: ScrollEdgeEffectStyle?, for edges: Edge.Set) -> some View
```
**`searchSelection(_:)`** — Binds the selection of the search field associated with the nearest searchable modifier to the given ``TextSelection`` v
```swift
nonisolated func searchSelection(_ selection: Binding<TextSelection?>) -> some View
```
**`searchToolbarBehavior(_:)`** — Configures the behavior for search in the toolbar.  This modifier can be used to change the default behavior of a search
```swift
nonisolated func searchToolbarBehavior(_ behavior: SearchToolbarBehavior) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the label that is used in a section index to point to this section, typically only a single character long.  - Para
```swift
nonisolated func sectionIndexLabel(_ label: Text?) -> some View
```
**`sectionIndexLabel(_:)`** — Sets the title that is used for the section index label pointing to this section, typically only a single character long
```swift
nonisolated func sectionIndexLabel<S>(_ label: S?) -> some View where S : StringProtocol
```
**`sensoryFeedback(_:trigger:)`** — Plays the specified `feedback` when the provided `trigger` value changes.  For example, you could play feedback when a s
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T) -> some View where T : Equatable
```
**`sensoryFeedback(_:trigger:condition:)`** — Plays the specified `feedback` when the provided `trigger` value changes and the `condition` closure returns `true`.  Fo
```swift
nonisolated func sensoryFeedback<T>(_ feedback: SensoryFeedback, trigger: T, condition: @escaping (T, T) -> Bool) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping (T, T) -> SensoryFeedback?) -> some View where T : Equatable
```
**`sensoryFeedback(trigger:_:)`** — Plays feedback when returned from the `feedback` closure after the provided `trigger` value changes.  For example, you c
```swift
nonisolated func sensoryFeedback<T>(trigger: T, _ feedback: @escaping () -> SensoryFeedback?) -> some View where T : Equatable
```
**`sliderThumbVisibility(_:)`** — Sets the thumb visibility for `Slider`s within this view.  Use this modifier to override the default slider thumb visibi
```swift
nonisolated func sliderThumbVisibility(_ visibility: Visibility) -> some View
```
**`symbolColorRenderingMode(_:)`** — Sets the color rendering mode for symbol images.  - Parameter mode: The color rendering mode, or nil to use the   defaul
```swift
nonisolated func symbolColorRenderingMode(_ mode: SymbolColorRenderingMode?) -> some View
```
**`symbolVariableValueMode(_:)`** — Sets the variable value mode mode for symbol images within this view.  - Parameter mode: The symbol variable value mode 
```swift
nonisolated func symbolVariableValueMode(_ mode: SymbolVariableValueMode?) -> some View
```
**`tabBarMinimizeBehavior(_:)`** — Sets the behavior for tab bar minimization.  The following TabView minimizes its tab bar when scrolling in the 'Numbers'
```swift
nonisolated func tabBarMinimizeBehavior(_ behavior: TabBarMinimizeBehavior) -> some View
```
**`tabViewSearchActivation(_:)`** — Configures the activation and deactivation behavior of search in the search tab.  Use this modifier on a ``TabView`` to 
```swift
nonisolated func tabViewSearchActivation(_ activation: TabSearchActivation) -> some View
```
**`task(id:name:executorPreference:priority:file:line:_:)`** — Adds a task to perform before this view appears or when a specified value changes.  This method behaves like ``View/task
**`textInputFormattingControlVisibility(_:for:)`** — Define which system text formatting controls are available.  - Parameters:   - visibility: Whether the control may becom
```swift
nonisolated func textInputFormattingControlVisibility(_ visibility: Visibility, for placement: TextInputFormattingControlPlacement.Set) -> some View
```
**`windowResizeAnchor(_:)`** — Sets the window anchor point used when the size of the view changes such that the window must resize.  In SwiftUI life c
```swift
nonisolated func windowResizeAnchor(_ anchor: UnitPoint?) -> some View
```
**`writingDirection(strategy:)`** — A modifier for the default text writing direction strategy in the view hierarchy.  To control the writing direction expl
```swift
nonisolated func writingDirection(strategy: Text.WritingDirectionStrategy) -> some View
```

### `WorldTrackingLimitation` — 5 new members

**`==(_:_:)`** — Returns a Boolean value indicating whether two values are equal.  Equality is the inverse of inequality. For any values 
```swift
static func == (a: WorldTrackingLimitation, b: WorldTrackingLimitation) -> Bool
```
**`hash(into:)`** — Hashes the essential components of this value by feeding them into the given hasher.  Implement this method to conform t
```swift
func hash(into hasher: inout Hasher)
```
**`hashValue`** — The hash value.  Hash values are not guaranteed to be equal across different executions of your program. Do not save has
```swift
var hashValue: Int { get }
```
**`orientation`** — The device capabilities of tracking orientation changes in all dimensions are currently limited.
```swift
static let orientation: WorldTrackingLimitation
```
**`translation`** — The device capabilities of tracking translation changes in all dimensions are currently limited.
```swift
static let translation: WorldTrackingLimitation
```
